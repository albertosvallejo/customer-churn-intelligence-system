# STATUS

## Project
Daily Customer Churn Predictor – VivaMarket Brasil

## Current state

**2026-09-04 live validation update (post-V15.4, evidence-based):** after the documentary-only V15.4 reconciliation, a fresh real runner validation was executed and finished `PASS` for `diag-20260904-004048-customer-churn-intelligence-system`. This proves a current end-to-end Churn validation pass exists beyond the historical-documentary baseline. The same pass also verified the runner evidence fix: `EVIDENCE_DIR` was populated with the normal result bundle instead of remaining empty.

**CURRENT_LIVE_JOB_EVIDENCE**
- `LATEST_FRESH_CHURN_JOB_ID=diag-20260904-004048-customer-churn-intelligence-system`
- `STATUS_JSON_STATE=PASS`
- `SUMMARY=Validation lifecycle completed.`
- terminal stdout stages confirmed: `BUILD=PASS`, `STARTUP=PASS`, `READINESS=PASS`, `PROJECT_VERIFY=PASS`, `DEMO_VERIFY=PASS`, `CLEANUP=PASS`, `VALIDATION_PASS=PASS`, `RESULT=PASS`
- `EVIDENCE_DIR_POPULATED=YES`
- populated evidence bundle confirmed with at least: `job.json`, `job.final.json`, `status.json`, `stdout.log`, `stderr.log`, `summary.txt`, `compose_guard.txt`, `pre_stdout_trace.log`, `pre_stdout_stderr.log`

**CURRENT_INTERPRETATION**
- `FINAL_CHURN_E2E_PASS=YES` is now supported by fresh job evidence.
- `CHURN_E2E_FINAL=PASS_CONFIRMED_BY_FRESH_RUN`
- The 2026-09-01 V15.4 note remains preserved below as the historical documentary state before this fresh validation was executed.

**2026-09-01 Churn documentary state reconciliation note (V15.4 scope, documentary-only):** this pass is limited to reconciling the Churn documentary state against already existing host evidence and historical job evidence. No deploy, restart, systemd reconfiguration, permission change, runner/bootstrap/client modification, new architecture, hardening, optimization, or new Churn job belongs to this scope.

**DOCUMENTED_BASELINE**
- Historical V15.3 baseline preserved for traceability:
  - `V1_DECISION=REMOVE_OPTIONAL_SYSTEMD_HARDENING`
  - `FINAL_HOST_DEPLOY_AFTER_THIS_DECISION=NOT_RECORDED`
  - `FINAL_CHURN_E2E_PASS=NOT_RECORDED`
  - `CHURN_E2E_FINAL=OPEN`
- This baseline is preserved as historical state and is **not** rewritten retrospectively.

**LIVE_HOST_EVIDENCE**
- Read-only host verification already obtained for the effective user unit:
  - `UNIT=/home/repro-runner/.config/systemd/user/repro-validation-runner.service`
  - `UNIT_SHA256=8591d10de4b52d9faadae4d1b8eb18f935e18a5b934ec5d5981dc4bc5438c7dc`
  - `UNIT_MTIME=2026-08-27 06:20:40 UTC`
  - effective content shows `EnvironmentFile=/srv/repro-validation-runner/config/runner.env`, `ExecStart=/srv/repro-validation-runner/bin/repro-runner-host.sh watch`, `WorkingDirectory=/srv/repro-validation-runner`, and no additional optional systemd hardening directives.
- Read-only service state already obtained for that same user service:
  - `MainPID=3791220`
  - `ExecMainStartTimestamp=Thu 2026-08-27 06:21:02 UTC`
  - `LoadState=loaded`
  - `ActiveState=active`
  - `SubState=running`
  - `FragmentPath=/home/repro-runner/.config/systemd/user/repro-validation-runner.service`
- Therefore the following states are now documented as proved by host evidence:
  - `FINAL_HOST_DEPLOY_AFTER_THIS_DECISION=CONFIRMED`
  - `SYSTEMD_OPTIONAL_HARDENING_ACTUAL_STATE=REMOVED`

**JOB_EVIDENCE**
- Latest already existing Churn job evidenced in this reconciliation scope:
  - `LAST_CHURN_JOB_ID=20260827-082056-customer-churn-intelligence-system`
  - `CLAIM_PRESENT=YES`
  - `RESULT_DIRECTORY_PRESENT=YES`
  - result directory content: `job.json` only
  - `STATUS_PRESENT=NO`
  - `SUMMARY_PRESENT=NO`
  - `FINAL_STATE_PRESENT=NO`
  - `STDOUT_PRESENT=NO`
  - `STDERR_PRESENT=NO`
  - `HOST_JOBDIR_PRESENT=NO`
  - `LAST_CHURN_JOB_TERMINAL_STATE=NONE_RECORDED`
- Lifecycle stages documented from evidence only:
  - `REQUEST=PASS`
  - `CLAIM=PASS`
  - `CONTRACT=UNKNOWN`
  - `SOURCE_VALIDATION=UNKNOWN`
  - `ISOLATED_COPY=UNKNOWN`
  - `SECURITY_GUARD=UNKNOWN`
  - `RESOURCE_GATE=UNKNOWN`
  - `ROOTLESS_DOCKER_ACCESS=UNKNOWN`
  - `BUILD=UNKNOWN`
  - `STARTUP=UNKNOWN`
  - `READINESS=UNKNOWN`
  - `PROJECT_VERIFY=UNKNOWN`
  - `CLEANUP=UNKNOWN`
  - `RESULT=NOT_RECORDED`

**INFERENCE**
- The host evidence proves that the optional systemd hardening was removed and that a post-decision host deploy exists.
- The currently cited job evidence does **not** demonstrate a full E2E PASS.
- Therefore `FINAL_CHURN_E2E_PASS=NO` means `no PASS E2E demonstrated`, not `E2E FAIL demonstrated`, and `CHURN_E2E_FINAL` remains `OPEN`.

**PLAN_VIGENTE:** `PLAN_ACCION_REPRODUCIBILIDAD_CHURN_V15.4.md`

**PROJECT_HISTORY_CONTINUITY:** `ACTIVE`
- `PROJECT_HISTORY_CONTINUITY=ACTIVE`
- `DETAILS=_private/_docs/PROJECT_HISTORY_CONTINUITY.md`
- `IMPLEMENTATION_MODE=DOCUMENTARY_CONTINUITY_LAYER`
- `SOURCE_PRECEDENCE_RULE=ENFORCED_IN_DOCUMENTATION`
- `MISSING_DAILY_MEMORY_RECOVERY=2026-08-31_AND_2026-09-01_MARKED_RECONSTRUCTED`

**2026-08-21 RH closure note:** RH is now closed at workspace/documentary level. RH-7 reused only the already approved RH-6 evidence and therefore closed with `surfaces = PASS`, `run_date = PASS`, `invariants = PASS`, `synthetic_demo = PASS`, and `staging untouched = PASS` without reopening visual QA, redeploying, or changing functional code. RH-8 then aligned the active docs and plan to the final runtime truth: FastAPI/Uvicorn is the only publicable HTTP runtime; legacy executable HTTP elements remain retired from the public repo line; preserved shared logic stays in `src/api/churn_service.py`; the final post-cleanup suite baseline recorded for RH is `165 passed`; and the rollback checkpoint `_private/rh/checkpoint_pre_rh_20260820_185636_UTC` remains the recovery anchor. Canonical state is now `P0/P1/P2 DONE`, `DASHBOARD_REVIEW_2 CLOSED`, `MANUAL REVIEW ACCEPTED`, `DRIVE SYNC MATCH`, `RH CLOSED`, `OR DEFERRED`.

**2026-08-20 active-plan correction note (historical, superseded by RH closure on 2026-08-21):** `_private/PLAN_ACCION_REPRODUCIBILIDAD_CHURN_V15.md` remains the governing file, but its active internal version is now `15.1`. That pass was editorial only and removed/marked residual executable references inherited from V14 so they could not be misread as live instructions. Historical state at that moment: `P0/P1/P2 DONE`, `DASHBOARD_REVIEW_2 CLOSED`, `MANUAL REVIEW ACCEPTED`, `DRIVE SYNC MATCH`, `RH OPEN`, `OR DEFERRED`. Current canonical state is the newer one recorded above: `RH CLOSED`, `OR DEFERRED`.

**2026-08-20 manual-review closure note:** Dashboard Review 2 manual-review correction cycle is now closed at workspace scope. The Instructor manually accepted the final candidate for `run_date=20260727` after reviewing Dashboard, Tested Actions, and New Actions. Final closure evidence lives in `_private/dashboard_review_2/manual_review/final_acceptance_20260820/`. No further functional/visual/layout changes or redeployments are authorized in this closed review line; only documentary closure and non-destructive workspace→Drive synchronization were authorized after acceptance.
**2026-08-18 MP-B gate note:** Alberto accepted the synthetic-namespace correction as the green technical gate for `MPB-01` to `MPB-03`, but explicitly did **not** close MP-B. The recoverable green baseline is now `_private/dashboard_review_2/mp_b/checkpoint_20260818_0455_UTC_partial_state`, documented inside `BASELINE_GREEN_ACCEPTED_20260818.md` and backed by `validation_after_fix/` (`9 passed` isolation, `43 passed` focused, `176 passed` full suite, idempotent bundle hashes). The local static/frontend tranche for `MPB-04` to `MPB-09` is now implemented and regression-covered on the workspace baseline (`32 passed` focused UI/view-model battery, `195 passed` full suite), but the ticket family is still **not** closed because served-route/browser evidence remains pending before any rebuild/deploy checkpoint. `MPB-10` remains pending. No `RH`, `OR`, systemd, rebuild/deploy, or historical Drive executable changes are authorized in this block.

**2026-08-18 MP-B execution note:** Alberto issued the literal approval `MP-A APPROVED` on `2026-08-18`, explicitly approving the final MP-A documentary hashes (`MOCKUP_PARITY_AUDIT.md` `5c7197f091c4c45011563729ae4e6848aa56118853caedc861e57d1dd5ad0a66`, `MP_CODE_BINDING_TABLE.md` `c56b43e0dbf5af67fbea59b624d46a009dfa13a263f990b71ae0122ce1dac57b`, `MOCKUP_PARITY_IMPLEMENTATION_PLAN.md` `116723a28cd3de93fe9450beb47795e867cae21c16b34c257dc4caa299cdb4ef`) and unlocking local MP-B implementation on the workspace baseline only. Active in-flight scope is code/tests only: the synthetic Phase 7 fixture has been enriched with business recommendations and coherent previous-action comparators for the two winning campaigns, the post-test decision queue is being narrowed to business-decidable winners only, and the reviewed frontend now moves toward business-facing `Accept / Reject` controls plus `5/page` history pagination and CSV export. No `RH`, `OR`, systemd, runtime deployment, or historical Drive executable changes are authorized in this block.

**2026-08-17 active-plan traceability note:** `_private/PLAN_ACCION_REPRODUCIBILIDAD_CHURN_V14.md` was replaced with the Alberto-provided revised V14 and verified byte-exact against SHA-256 `64df6ea60ba7417cb6a9985d6953bd6e4b393bbe471b721418e00e68255fcc59`. `ACTIVE_PLAN.md` continues to point to this V14 as the governing plan. This revision now records the human gate `MP-A`, clarifies that a generic `adelante` authorizes only reading/audit work, and explicitly forbids implementing mockup-parity changes without the literal approval string `MP-A APPROVED`.

**Canonical publication-flow note:** the single source of truth for the transition `workspace VPS -> Drive bajo petición -> copia local del Project Author -> subida final a GitHub -> builds/digests por GitHub Actions` is `_private/PLAN_ACCION_REPRODUCIBILIDAD_CHURN_V12.md`, section `2.1`. `STATUS.md` only references that rule and must not redefine it.

**2026-08-15 priority clarification now recorded in the governing plan:** section `2.1` now explicitly prioritizes the project as a Data Science / portfolio deliverable over additional infrastructure hardening, approves the public-demo channel as `Quick Start + README screenshots/GIF` rather than an always-on Traefik URL, and clarifies that public read access on `/customer-churn/dashboard`, `/customer-churn/tested-actions-approval`, and `/customer-churn/new-actions-testing` must remain token-free. `X-Phase7-Token` is now documented as write-only for `POST /phase7/actions/decision` and `POST /phase7/actions/post-test-decision`.

Phase 1 / v1.0.0 has historical traceability in the project documentation as an earlier baseline, but that historical note is **not** the governing rule for the current workstream. The current workstream follows the canonical transition rule above: active implementation happens in the VPS workspace, Drive replication occurs only on request once the content is reproducible, and no pre-GitHub image build or digest-generation step is treated as part of the current workspace/Drive phase. As of 2026-08-15, the governing plan also makes explicit that the final GitHub upload is a **single final event, not an incremental one**: any pre-publication Phase 10 evidence produced from the workspace must be reported as `validación mecánica local, sin digest de release`, and cannot be used as a substitute for real CI release digests when closing CP5. Phase 2 / v2 has now progressed through `NB09` on the canonical `V2C` line, and the local publication layer has now been hardened as well. On 2026-05-12, local-only Phase 2 / Phase 3 workspace updates resumed with an upgraded NB09 Plotly dashboard, a local model card, an illustrative ROI simulation, and a README expansion that now includes `Project Evolution`. On 2026-05-18, the deferred Drive replication was resumed and the pending workspace/project files were synchronized and verified through the mounted Drive path `/data/gdrive`. Later the same day, the new n8n support API endpoints were implemented locally: `GET /explainability/latest` and `POST /coupons/generate`. After the user approved a port change, the service was standardized on port `62881` and a container-restart watchdog was added through the persistent OpenClaw cron job `ensure-churn-api-62881`. On 2026-05-19 evening, the local Phase 3 minimum-publication layer was revalidated end-to-end through real execution of `NB05`–`NB09`, and the stakeholder-facing Model Card / ROI reporting was strengthened in the canonical local workspace without depending on the current rclone incident. On 2026-05-31, the project state was explicitly re-clarified with the user: this is a portfolio / demo system, not a live production deployment. The code, workflow logic, API, Postgres persistence, and OneSignal authentication are real implementation artifacts, but campaign outcomes for Phase 4 measurement are generated synthetically. Reviewing the latest Phase 4 evidence now shows the effective execution position is: Block B closed; Block C closed under the demo/synthetic measurement rule, because the local synthetic 30-day `retention_events` baseline and aligned `retention_actions_synthetic_30d.parquet` / KPI pipeline were already generated and validated, including closed-conversion evaluation logic and holdout-lift reporting over the synthetic dataset. Therefore the next active workstream is no longer “close Block C,” but continue from the already-closed synthetic measurement baseline with: Block D still pending/early (governance and drift monitoring are not yet evidenced as active weekly outputs or Model Card v3), Block E demo already implemented locally, and Block F not yet started as a documented benchmark workstream.

For the current Phase 10 execution position, steps `9-11` now have a new **mechanical-only** evidence slice dated `2026-08-15`, explicitly classified as `validación mecánica local, sin digest de release`: private staging returned `health 200`, the dashboard surface returned `200`, the cold/parallel concurrency check completed `20/20`, and the inspected logs were clean with no secret leakage observed. In the same pass, a real packaging bug was identified in `deploy/compose.prod.yml`: the API healthcheck still called `scripts/http_healthcheck.py` with the unsupported `--url` flag even though the script only accepts a positional URL (or `HEALTHCHECK_URL`). That mismatch has now been corrected in the tracked production manifest. This evidence is useful for Phase 10 staging readiness, but it does **not** close CP5 and does **not** replace the missing final release-manifest / digest evidence required after the final GitHub publication + CI release flow.

For the current Phase 5 execution position, the project is now structurally well prepared but still gate-constrained: verified shadow evidence stands at `4/14` valid shadow days, `4/4` reconciled matches, `0` divergences, and `0` pending human decisions. The main operational/API/reporting surfaces for Block G are now implemented and tested, but the hard milestone progression remains limited by missing calendar-valid shadow accumulation rather than by missing infrastructure. On 2026-06-19, a traceability incident review confirmed a more specific risk: calendar time advanced past 2026-06-08, but the canonical local `agent_decision_log` still only evidences four shadow cycles through `2026-06-08`, so Phase 5 cannot be treated as progressed just because days elapsed. The project now has an explicit control requirement: verify canonical DB source, latest logged cycle date, and stale-log gap before trusting any Phase 5 progress claim.

Phase 6 is now no longer only approved-on-paper: on 2026-07-21 the first executable implementation cut was started locally and stabilized. The project now has a formal spec at `docs/phase6_spec_dynamic_evidence_system.md`, an executable bootstrap builder at `scripts/build_phase6_catalog.py`, reusable catalog-generation logic in `src/evidence/catalog_builder.py`, regression coverage in `tests/test_phase6_catalog_builder.py`, and the first immutable seed-baseline catalog/report pair at `data/processed/evidence_catalog_20260721.json` and `reports/phase6_evidence_catalog_summary_20260721.md`. This cut intentionally stays inside the approved initial guardrails: monthly cadence, dashboard-based single approver (`<project-owner-email>`), simulated-only A/B launch mode, and inherited Phase 5 references treated only as seed evidence rather than as the full dynamic system.

For the current Phase 7 execution position, Fase 7.1 is now explicitly closed at plan + code + evidence level: the active V12 plan already records deterministic NB01→NB09 clean-smoke closure, source-dataset checksum pass, and model-lineage closure with real `git_sha` in clean-room manifests, while the remaining Phase 7 governance/reporting workstream itself is not reopened by that documentary closeout. The earlier integrated-actions refresh blocker is no longer open: the persisted governance/stat lifecycle state is now preserved across `refresh=true` rebuilds, and the direct public statistical-launch route has already been degraded in favor of the governed launch-request flow. The current verified governance baseline now also closes sub-phase `7.1.7`: the live sensitive Phase 7 write routes are aligned to a **single business approver token model**, including `POST /phase7/actions/decision`, `POST /phase7/actions/post-test-decision`, `POST /phase7/stat-launch-requests`, and `POST /phase7/stat-launch-requests/execute`. On 2026-08-04, the Phase 7 business-dashboard rebrand/split was implemented locally: the old business-facing pages `/phase7/reporting` and `/phase7/review/page-2-2` were cleanly replaced by `/customer-churn/dashboard`, `/customer-churn/tested-actions-approval`, and `/customer-churn/new-actions-testing`, with matching `/data` endpoints, updated business copy/layout, preserved token gating only on the two protected approval surfaces, and no change to the protected write endpoints/security model. On 2026-08-08/11, the project then entered a reproducibility-focused CP0/CP1/CP2/CP3 hardening block over the local workspace baseline: CP0 is evidence-complete with green suite, functional transcript, SHA-256 manifest, frontend source mapping, and read-only exposure audit; CP1 additionally closed endpoint inventory, compact HTTP contracts, artifact I/O map, file-level migration plan, proposed repo tree, explicit demo-state definition, dependency/runtime freeze policy, and the written decision that PostgreSQL is **not** required for the first reproducible public demo baseline. After isolating and fixing a cross-suite SQLite temp-path leak between `tests/contract/test_phase7_http_contract.py` and `tests/test_churn_service.py`, the full root-level suite is now back to green at `158/158` and the earlier `10` failing `tests/test_churn_service.py` endpoints are no longer open. Alberto explicitly approved the CP1 direction/architecture, so CP1 is formally closed from the local workspace side. CP2 is now also formally closed from the local workspace side: the project now has explicit `api + web` packaging artifacts (`Dockerfile.api`, `Dockerfile.web`, `web/nginx.conf`, `src/api/app.py`, `docker-compose.yml`, `docker-compose.cp2.local.yml`), explicit health/readiness checks, an executable smoke verifier at `scripts/demo_verify.sh`, and a documented local non-Docker smoke path in `docs/cp2_packaging_runbook.md`. Fresh local verification against the CP2 synthetic-demo baseline passed through `PYTHONPATH=src python3 -m api.app`, `scripts/demo_verify.sh`, and `scripts/http_healthcheck.py`. CP3 is now also formally closed from the local workspace side: the active backend runtime has been switched to FastAPI/Uvicorn through `src/api/fastapi_app.py` + `src/api/app.py`, `requirements.txt` now includes `fastapi`, `uvicorn`, and `python-dotenv`, the approved `/customer-churn/...` route family and token-gated operator behavior remain preserved, deprecated `POST /phase7/stat-tests/launch` still returns `410 Gone`, and `MODE=synthetic_demo` continues to run locally with `LLM_MODE=disabled` and no OpenAI key. Fresh local CP3 verification passed through the FastAPI runtime using `PYTHONPATH=src python3 -m api.app`, `scripts/demo_verify.sh`, `scripts/http_healthcheck.py`, and an explicit `410` check on the deprecated launch route. The host-side SSH/port exposure re-verification remains intentionally deferred to the CP5 gate before any VPS / Traefik / systemd touch and is not treated as a CP1/CP2/CP3 blocker, while Docker image/build execution itself remains deferred to a Docker-capable external environment rather than treated as an open local blocker. On 2026-08-01 afternoon, the remaining minor documentary follow-up for the formal Phase 7 close was also resolved as far as this runtime allows: the literal Block 3 transcript is now pinned to the preserved real suite artifacts `tmp_test_outputs/phase7_block3_literal_transcript_10_tests.txt` and `tmp_test_outputs/phase7_block3_literal_transcript_28_tests.txt` (exact `10 + 28 = 38` names), the current shared Phase 7 regression baseline for `tests.test_churn_service + tests.test_phase7_review_page + tests.test_phase7_integration` was re-run live and now stands at `54/54`, and the post-`7.2.4` delta that can be proved from preserved suite artifacts is exactly three tests: `test_phase7_review_token_rejects_launch_request_and_execute_routes` (`7.1.7`), `test_review_page_view_model_reuses_preloaded_integrated_payload_for_post_test_queue` (`7.2.6`), and `test_load_integrated_actions_uses_short_ttl_cache` (`7.2.6`). A full commit-by-commit attribution via `git log` / `git diff` could **not** be completed because neither the workspace copy nor the mounted Drive copy is a Git work tree in this runtime, so the historical `51→55` claim cannot be proved from Git here and remains only partially inferable from preserved transcripts and docs. The old OpenAI key revocation step is now closed at documentary level: the same legacy key still remains referenced in both project `.env` files (`/data/.openclaw/workspace/.../.env:19` and `/data/gdrive/Portfolio/.../.env:19`), but Alberto has already revoked it in the OpenAI dashboard using the masked fingerprint `[REDACTED]`. The env values were intentionally left untouched for now because the revoked key is already dead; if a future replacement key is generated, both env files should then be updated together. On 2026-07-31, Block 3 (`7.3.1` + `7.3.2`) was then implemented locally: Phase 7 now supports an explicit `MODE=synthetic_demo` path with dedicated namespace resolution under `data/synthetic_demo/`, canonical synthetic artifact prefixes `synthetic_demo__*` to avoid basename-only collisions, a seeded synthetic full-cycle package (snapshot → drafts → stat history/test runs → dashboards), and a fixed non-dismissible warning banner on both dashboard surfaces (`/phase7/reporting` and `/phase7/review/page-2-2`) whenever they consume synthetic-demo artifacts. Later the same morning, an **independent direct inspection on the GDrive/production copy** confirmed that the single-business-approver token gate is indeed enforced there as well for `POST /phase7/stat-launch-requests` and `POST /phase7/stat-launch-requests/execute` through `_require_phase7_review_token(self)`. Later the same day, the previously failing public-exposure burst path for `/phase7/reporting` was reported stable at the concurrency level: after the host-side Traefik transport adjustment confirmed by Alberto, the public 10-way HTTPS burst reportedly completed cleanly twice in a row, and an additional agent-run 20-way public regression via Traefik also completed `20/20` with `HTTP 200` responses in `1.316683s`–`1.698591s`. On 2026-08-01, the last remaining `7.2.6` blocker on the protected review page was then isolated and fixed in Python code without further proxy changes: the live service was confirmed to run from the Drive-mounted code path (`PYTHONPATH=/data/gdrive/Portfolio/daily-customer-churn-predictor/src` in this runtime; historically the same production project is documented host-side under `/mnt/gdrive/Portfolio/daily-customer-churn-predictor/`), and the protected review-page cold path was traced to the Drive-backed integrated-actions artifact `data/processed/phase7_integrated_actions_20260733.json`. Before the fix, page 2.2 performed a redundant second read of that artifact inside one request and had no in-memory shield against concurrent cold reads; direct backend concurrency reproduced the failure (`10/10` timeouts on a cold 10-way direct burst), while a single warm-up immediately made the 10-way Traefik burst pass, proving a cold-start/shared-I/O bottleneck rather than a Traefik transport defect. The code now passes the already-loaded integrated payload into `build_phase7_post_test_decision_queue(...)` and adds a short thread-safe TTL cache for integrated-actions loads. Fresh cold-start validation after service restarts now passes cleanly: direct backend sequential `10/10` (`attempt 1 = 0.462747s`, attempts `2-10 = 0.002164s–0.003420s`), Traefik sequential `10/10` (`0.899779s–1.163130s`), and Traefik parallel `10/10` (`1.563530s–2.997765s`). The previously failing cold direct backend 10-way parallel burst also now passes `10/10` in `0.440035s–0.458245s`, confirming the backend cold-start bottleneck on `/phase7/review/page-2-2` is resolved.

Later the same night, the Sub-phase 6.2 prototype was extended further and the first 6.3 validation baseline was prepared. The project now also has `src/evidence/recommendation_builder.py`, execution entrypoint `scripts/build_phase6_recommendations.py`, first catalog-derived report `reports/phase6_dynamic_evidence_recommendations_20260721.md`, automated validation `tests/test_evidence_sourcing.py`, and manual validation checklist `reports/phase6_validation_checklist_20260721.md`. Under the current seed baseline, `INT-02` is the only recommendation that reaches `approval_gate_eligible`, while `INT-01` and `INT-04` remain `evidence_enrichment_only` because low confidence still blocks autonomous approval despite high actionability.

On 2026-07-24, the first hardening pass defined by Phase 6 plan v6 was implemented locally and verified. The Phase 6 baseline now blocks same-date catalog overwrite attempts with an explicit `FileExistsError`, exposes `--run-date` in both CLI entrypoints, fails cleanly when no catalog exists for the requested run date, strengthens allowlist/seed validation with readable governance errors, adds public business-rule docstrings to the evidence builders, extends automated coverage to the missing `published`, `high -> tier_1`, stable tie-break, empty-catalog, and no-catalog scenarios, and updates the manual validation checklist accordingly. Real execution also produced a fresh validated snapshot/report set for `20260724`.

Later the same day, a real bug was detected in the new P3.3 fallback logic: recommendation generation without `--run-date` was still forcing today's date instead of using the latest available catalog snapshot. That bug has now been corrected so `build_and_write_recommendations()` resolves the latest catalog when no explicit run date is provided, derives the effective run date from that snapshot filename, and writes the recommendations report against the actual catalog date in use. Automated coverage now includes the exact no-`run_date` fallback scenario.

Also on 2026-07-24, the corrected planning document was promoted again to `_private/phase6_action_plan_v8.md` so the operational plan now explicitly captures the main hardening block, the later real-world fallback fix, and the subsequent correction of the first clock-sensitive regression test.

After Alberto updated the planning reference again from Drive to `_private/phase6_action_plan_v9.md` and explicitly approved continuing into Sub-phase 6.4, the first 6.4 implementation slice was started locally. The project now has a new Phase 6 integration module `src/evidence/phase6_integration.py`, a proposal build entrypoint `scripts/build_phase6_action_proposals.py`, new API endpoints in `src/api/churn_service.py` for proposal retrieval / decision logging / action-history retrieval, and a new dated proposal artifact pair `data/processed/phase6_action_proposals_20260724.json` + `reports/phase6_action_proposals_summary_20260724.md`. This first slice keeps the approved governance boundary: only approval-gate-eligible recommendations for `INT-01` / `INT-02` / `INT-04`, simulated deployment mode only, mandatory human decision, and append-only action history persisted to `data/processed/action_history_log.parquet` once a decision is recorded.

That first slice has now been extended to close the full Sub-phase 6.4 in simulated mode. The project now also includes a simulated A/B launch path (`scripts/launch_phase6_ab_test.py`), a KPI status builder (`scripts/build_phase6_kpi_status.py`), a versioned n8n-ready payload builder (`scripts/build_phase6_n8n_payload.py`), new API endpoints for launch / KPI status / n8n payload, and a second append-only history event type (`ab_test_result`) linked to `data/processed/phase6_ab_test_runs.parquet`. A bug-audit pass over those new changes surfaced and fixed a real regression on inherited Phase 5 agent endpoints, after which the relevant Phase 6/6.4 validation suite passed 28/28 tests and the operational simulated chain was rebuilt successfully on the 20260725 snapshot set.

A new review document `_private/phase6_action_plan_v10.md` is now ready as the updated operational reference for Alberto review: it supersedes v9 only once Alberto validates the new documented closure of 6.4.

A subsequent real validation pass on 2026-07-24 exposed a regression introduced during the 6.4 slice: several existing Phase 5 agent endpoints in `src/api/churn_service.py` had been left out of the final request handler flow, producing 404/500 failures in `tests.test_churn_service`. That regression was fixed by restoring the `/agent/status`, `/agent/status/daily`, `/agent/status/shadow-monitor`, `/agent/status/phase5`, `/agent/decisions/shadow`, `/agent/decisions/shadow/run`, and `/agent/decisions/shadow/reconcile` routes, reconnecting them to the existing Phase 5 loaders, restoring decision-log counters (`decision_log_table`, `pending_human_decision_count`, `shadow_log_count`), and reintroducing the missing Phase 5 support modules `src/pipeline/phase5_daily_status.py` and `src/pipeline/phase5_shadow_monitor.py` required by those endpoints.

The corrective pass also confirmed that the append-only catalog guardrail is behaving as designed: rerunning `scripts/build_phase6_catalog.py --run-date 20260724` fails with the expected `FileExistsError`, while a clean next-date smoke run (`20260725`) successfully rebuilt the full Phase 6 chain: catalog, recommendations, and approval proposals.

On 2026-07-24, Sub-phase 6.5 was then closed at documentary scope. `README.md` now includes an explicit Phase 6 closure section, the historical naming collision was resolved by renaming `reports/phase6_intervention_recommendations_20260718.md` to `reports/phase5_step6_intervention_recommendations_20260718.md`, limitation #12 was updated from an open static-recommendation gap to a mitigated-but-not-resolved real-pilot boundary, and the dated closure artifact `reports/phase6_case_study_signoff_20260724.md` was published. The honest scope remains unchanged: Phase 6 is closed as a dynamic evidence + simulated decision-support system, not as a completed real-customer pilot.

## Last completed step
- On 2026-08-18 morning, the namespace-isolation correction for the synthetic demo was accepted as the green technical gate for `MPB-01` to `MPB-03`. The new recoverable baseline is `_private/dashboard_review_2/mp_b/checkpoint_20260818_0455_UTC_partial_state`, with explicit acceptance marker `BASELINE_GREEN_ACCEPTED_20260818.md` and validation evidence in `validation_after_fix/`: `9 passed` isolation tests, `43 passed` focused suite, `176 passed` full suite, and identical first/second bundle hashes in `05_hash_comparison.json`.
- On 2026-08-18 morning, the local static/frontend tranche for `MPB-04` to `MPB-09` was completed on the workspace baseline. The dashboard/reporting surface now exposes canonical snapshot-date semantics, KPI chip + summary-card hierarchy, and business-facing meta/CTA copy; New Actions now blocks missing recommendation context and differentiates write-auth states; shared pagination controls now expose clearer keyboard/screen-reader hooks; and `web/static/css/dashboard-app.css` now acts as the unified desktop/mobile shell across Dashboard / Tested / New Actions. Focused validation passed with `python3 -m pytest tests/test_phase7_reporting_page.py tests/test_phase7_review_page.py tests/test_fastapi_app_contract.py tests/ui/test_phase7_visual_desktop.py tests/ui/test_phase7_visual_mobile.py tests/ui/test_phase7_business_copy.py tests/ui/test_phase7_accessibility.py tests/ui/test_tested_actions_flow.py tests/ui/test_phase7_history_controls.py -q` -> `32 passed`, and the full suite passed with `PYTHONPATH=src python3 -m pytest -q` -> `195 passed`. Served-route/browser evidence is still pending, so `MPB-04` to `MPB-09` are not yet documentary-closed.
- On 2026-08-18 early morning, the first verified MP-B implementation slice was completed locally on the editable workspace baseline: `data/synthetic_demo/synthetic_demo__phase7_action_drafts_20260727.json` and the synthetic bundle builder were updated so the versioned fixture — not the frontend — now carries clear business recommendations for the three new proposals plus coherent synthetic previous-action metadata for the two winning tested campaigns. The reviewed Tested Actions frontend JS was also refactored toward business-language `Accept / Reject`, reject-reason enforcement, client-side `5/page` history pagination, and CSV export naming `phase7_history_<run_date>.csv`. Local verification now passes for the relevant modified regression surface with `MODE=real PYTHONPATH=src python3 -m pytest tests/test_phase7_review_page.py tests/test_phase7_reporting_page.py tests/test_phase7_integration.py tests/test_fastapi_app_contract.py tests/contract/test_phase7_http_contract.py -q` -> `43 passed`.
- On 2026-08-17, the active documentary V14 was refreshed from Alberto's attached file in `_private/PLAN_ACCION_REPRODUCIBILIDAD_CHURN_V14.md` and verified byte-exact with SHA-256 `64df6ea60ba7417cb6a9985d6953bd6e4b393bbe471b721418e00e68255fcc59`. `_private/ACTIVE_PLAN.md` was kept pointing to this V14 as the active governing plan. The traceability update now explicitly records that this revision introduces the human gate `MP-A`, that a generic `adelante` authorizes only read/audit work, and that no mockup-parity implementation is authorized without the literal approval `MP-A APPROVED`.
- On 2026-08-17 late night, the read-only MP-A repo-grounding pass was completed against the three canonical Drive sources under `_private/dashboard_review_2/`. Their SHA-256 hashes were locked (`a88fcaa7411de3fe91036559ffa0c20404b4ba220d927567f4f361f3b5803d7f`, `e0b49ea7ac9a38aa26e92201a401427b8a0f208efef8d15b142b90993347e857`, `78344e0af3998d9feaf4fba7bf23fefc564948126ccf759981de8043c902f1d0`), the real FastAPI/HTML/CSS/JS served files were inspected and hash-locked, and two derived audit artifacts were generated without touching product code: `_private/dashboard_review_2/mp_a/MP_A_SOURCE_LOCK_AND_SERVED_INVENTORY_20260817.md` and `_private/dashboard_review_2/mp_a/MP_A_GAP_TO_CODE_TRACE_MATRIX_20260817.md`. This closes the documentation-only bridge from MP-A findings to exact repo files/symbols/selectors/tests and does **not** authorize implementation.
- On 2026-08-17, Alberto requested a punctual sanitized Drive replica for external review of the real integrated Phase 7 business pages versus the original dashboard mockup. The active serving paths were verified first from `src/api/fastapi_app.py`: `/customer-churn/dashboard` -> `web/templates/dashboard.html`, `/customer-churn/tested-actions-approval` -> `web/templates/tested-actions-approval.html`, and `/customer-churn/new-actions-testing` -> `web/templates/new-actions-testing.html`. The requested HTML/CSS/JS/API context files plus a sanitized mockup-reference subset were then copied to `/data/gdrive/Portfolio/daily-customer-churn-predictor/_private/dashboard_review` and verified byte-for-byte against local source. A credential-pattern scan over the copied package returned no `.env` files, no credential-named files, and no common inline secret hits. Sync status for this review package: `Local: yes`, `Drive: yes`, `Verified: yes`.
- On 2026-08-17, the operational doubt around P0 vs staging was closed with evidence and the governing plan was updated accordingly. The investigation confirmed that P0 had in fact been implemented in code on 2026-08-15 (public-read GET behavior in `churn_service.py`, `fastapi_app.py`, and legacy review-page refresh alignment in `phase7_review_page.py`), but the staging artifact being observed had been created earlier than the relevant source-file mtimes, so it could not yet contain that fix. The evidence chain captured in today's project traceability is now: code chronology showing source mtimes later than the served staging artifact chronology, plus end-to-end runtime confirmation after rebuild that the read surfaces return `200` without token while the protected write decision route still returns `401` without token. The active V12 plan now records this closure in section-table row `12.1` and adds a new forward protocol rule in `13.12`: every future fix closure that affects a staging/VPS-served surface must report both source-file `mtime` and the real running container `Created` timestamp (`docker inspect --type=container ...`) rather than relying on `tests verdes` alone.
- On 2026-08-15, backlog `P2` was applied to the local reproducible codebase and verified by real tests. The business-facing Phase 7 HTML pages were visually aligned to the approved mockup under `assets/images/dashboard-mockup/`: `/customer-churn/tested-actions-approval` and `/customer-churn/new-actions-testing` now reuse the same sidebar feedback shell as the dashboard (star row, optional comments box, send-feedback button), use the exact mockup header casing (`Tested Actions Approval`, `New Actions Approval`), render the informational `i` control as the same button-style help affordance in `run-meta`, and match the mockup notice/copy on the tested-actions page. `/customer-churn/dashboard` was also aligned by removing the hero CTA buttons, switching the run-meta copy to `Latest campaign data: ... · KPI artifact generated at ...`, and capitalizing the hero status card title as `ONGOING CAMPAIGNS`. Synthetic-banner rendering is now centralized so synthetic-demo pages render the warning consistently from mode even if the warning string is not pre-populated in the page view model. The focused verification pass `python3 -m unittest tests.test_phase7_reporting_page tests.test_phase7_review_page tests.test_churn_service tests.contract.test_phase7_http_contract -v` finished green at `44/44`.
- On 2026-08-15, backlog `P1` was applied to the local reproducible codebase and verified by real tests. The shared Phase 7 historical mapping used by both `/customer-churn/dashboard` and `/customer-churn/tested-actions-approval` was normalized so the render layer accepts both legacy and current field names for the same business concepts: `guardrail` ↔ `guardrail_risk`, `test_period` ↔ `launch_at/closed_at`, and `decision_reason` ↔ `reason`. The reporting-page history builder now coerces those aliases into one canonical render shape before HTML generation, preventing blank cells and incorrect labels on historical rows. A focused render/snapshot-style regression was added in `tests/test_phase7_reporting_page.py` to prove mixed legacy/current payloads render visible period/reason/guardrail values without fallback blanks. The focused verification pass `python3 -m unittest tests.test_phase7_reporting_page tests.test_phase7_review_page tests.test_churn_service tests.contract.test_phase7_http_contract -v` finished green at `44/44`.
- On 2026-08-15, backlog `P0` was applied to the local reproducible codebase and verified by real HTTP tests. The read gate was removed from the HTML pages and GET data endpoints for `/customer-churn/tested-actions-approval` and `/customer-churn/new-actions-testing` in both the legacy `churn_service.py` path and the FastAPI path, while `X-Phase7-Token` was preserved only on `POST /phase7/actions/decision` and `POST /phase7/actions/post-test-decision`. The legacy review-page renderer was also aligned so its GET refresh URLs no longer append `token=`. Contract coverage now includes the four explicit P0 checks: GET pages without token -> `200`, GET data without token -> `200`, POST without token -> `401`, POST with wrong token -> `401`, plus valid-token POST acceptance. The focused verification pass `python3 -m unittest tests.contract.test_phase7_http_contract tests.test_churn_service tests.test_phase7_review_page -v` finished green at `39/39`.
- On 2026-08-15, the active V12 plan and the Phase 7 proposal builder were aligned to the latest Project Author decisions. The governing plan now includes `2.1.A Alcance priorizado del proyecto`, `2.1.B Canal de demostración pública`, and an explicit superseding note in `13.10` that resolves the internal contradiction against `13.9.2`: the three `/customer-churn/...` pages are documented as public read surfaces without token, while `X-Phase7-Token` remains restricted to the two write POST routes only. In parallel, `src/evidence/phase7_proposal_builder.py` was hardened so OpenAI key resolution depends only on the operator's local `.env` or process environment; the previous Drive fallback path was removed and missing-key behavior now fails closed with a clear BYOK-disabled message.
- On 2026-08-15, Fase 10 steps 9-11 were validated mechanically on private staging without touching Traefik, `churn-api.service`, or the live legacy route. The recorded result was `health 200`, dashboard `200`, cold/parallel concurrency `20/20`, and clean logs without visible secrets. This pass is explicitly classified as `validación mecánica local, sin digest de release`, so it advances only the technical staging-readiness side of CP5 rather than the final digest-backed gate. The same pass also surfaced a real manifest bug in `deploy/compose.prod.yml`: the API healthcheck was still using `python /app/scripts/http_healthcheck.py --url ...` even though `scripts/http_healthcheck.py` only accepts a positional URL or `HEALTHCHECK_URL`. That tracked manifest mismatch has now been corrected.
- On 2026-08-14, the two residual closure gaps from Fase 7.1 were closed locally without touching Fases 8/9/10/11/12. The papermill smoke runner now executes clean copies with a Git-aware reference root so `current_git_sha(...)` resolves the real commit during clean-room notebook runs, and new clean smokes at `/tmp/phase71_smoke_a_git` and `/tmp/phase71_smoke_b_git` both wrote `model_manifest_20260814.json` with `git_sha=650d9ba7850ca497cdc4cee67a0867ab481bb4f9` instead of `null`. Their tracked artifact hashes matched exactly again, including the manifest itself. The previously missing automated guards are also now present: one test forces an explicit checksum mismatch failure in `scripts/verify_source_dataset.py`, and another proves deterministic seed propagation in `src/notebook_runtime.py` through real Python and NumPy random operations. Full project regression was re-run end to end and now passes at `166/166` with `python3 -m pytest`.
- On 2026-08-14, Fase 7.1 of the active V12 reproducibility plan was completed locally at notebook-pipeline scope without touching Fases 8/9/10/11/12 beyond traceability updates. The project now has a central runtime config at `config/notebook_runtime.yaml`, enforced source-dataset checksum verification through `data/DATASET_MANIFEST.json` + `scripts/verify_source_dataset.py`, per-training `model_manifest_YYYYMMDD.json` generation from NB04, and NB01→NB09 deterministic smoke validation via two clean papermill executions with identical hashes across the tracked intermediate artifacts for run date `20260814`. Full project regression also passed at `164/164` with `pytest`.
- On 2026-08-14, a full documentation-consistency audit was executed over the active plan, `tech_doc`, `STATUS.md`, `README.md`, `DEPLOYMENT.md`, `SECURITY.md`, `docs/adr/ADR-001-reproducible-deployment.md`, and workspace `TASKS.md` so the real publication flow is written identically everywhere. The outcome is now explicit: `_private/PLAN_ACCION_REPRODUCIBILIDAD_CHURN_V12.md`, section `2.1`, is the only canonical source for the transition `workspace VPS -> Drive bajo petición -> copia local del Project Author -> subida final a GitHub -> builds/digests por GitHub Actions`; the rest of the documents now reference that section instead of redefining the full flow. This consistency pass is considered closed and must not be reopened as a pending item unless the Project Author explicitly decides to advance the final GitHub publication step.
- On 2026-08-12, the local frontend-integration validation pass was closed with real browser-backed evidence on the new `/customer-churn/...` surfaces. FastAPI now serves the new reproducible `HTML/CSS/JS` frontend directly on `/customer-churn/dashboard`, `/customer-churn/tested-actions-approval`, and `/customer-churn/new-actions-testing` using `web/templates/*` plus `/static/*`. Two real issues discovered only during browser validation were fixed in the same pass: (1) missing `run_date` propagation in the new frontend GET/POST flows (`web/static/js/base.js`, `web/static/js/new-actions.js`, `web/static/js/tested-actions.js`), which had caused the protected UI to render the wrong snapshot silently; and (2) a visual layout defect in the `.decision-note` / textarea block of `new-actions`, corrected in `web/static/css/dashboard-app.css`. The pass also produced real successful `201` evidence for both decision POST routes and archived browser captures under `reports/reproducibility/browser-smoke/`.
- On 2026-08-11, CP5 external reproducibility was verified successfully. The exact external terminal transcript is now preserved at `reports/reproducibility/CP5_EXTERNAL_VALIDATION_EVIDENCE.txt`, showing `docker compose up --build`, healthy `api + web` containers, `demo_verify.sh OK`, correct `200/401/200` route behavior for the `/customer-churn/...` surface, and clean `docker compose down` teardown from the external machine.
- On 2026-08-11, CP3 was formally completed from the local workspace side. The final local FastAPI/BYOK evidence is captured in `reports/reproducibility/03_cp3_fastapi_byok_audit.md`: the backend runtime now starts through FastAPI/Uvicorn, the approved route/token contract remains intact, synthetic demo still works locally with `LLM_MODE=disabled` and no OpenAI key, and a fresh isolated local run passed through `PYTHONPATH=src python3 -m api.app`, `scripts/demo_verify.sh`, `scripts/http_healthcheck.py`, and an explicit `410` check on `POST /phase7/stat-tests/launch`.
- On 2026-08-10, the CP1 local test-isolation blocker was resolved without production changes. The earlier root-level `pytest` failure (`10` failing `tests/test_churn_service.py` endpoints caused by a cross-suite SQLite temp-path leak) was isolated to module-scope temp DB setup in `tests/test_churn_service.py`, then fixed by moving the temp DB creation into `setUpClass`, rebinding `churn_service.DEFAULT_DB_URL`, and clearing the cached engine before the per-class HTTP server starts. The mixed-order reproducer `tests/contract/test_phase7_http_contract.py + tests/test_churn_service.py::TestChurnService::test_agent_daily_status_endpoint` now passes (`6/6`), and the full root-level suite now passes `158/158` in `10.66s`.
- On 2026-08-08, CP0 on the workspace baseline was materially closed without production changes. The failing baseline tests were minimally aligned to the current approved source-of-truth behavior (business-copy/label updates in Phase 4 tests plus the current synthetic banner/class expectation in Phase 7), `pytest` and `statsmodels` were installed in the local user environment to execute the real current suite, and the full project suite now passes with `153/153` (`reports/reproducibility/00_full_test_transcript.txt`). CP0 evidence was then expanded with a focused operator-flow subset (`4/4` in `reports/reproducibility/00_functional_matrix_transcript.txt`), an isolated ephemeral server concurrency/fresh-start read check (`reports/reproducibility/00_concurrency_restart_check.txt`), a full SHA-256 manifest (`reports/reproducibility/00_sha256_manifest.txt`, `260` entries), a frontend source correspondence map (`reports/reproducibility/00_frontend_source_map.md`), a security read-only baseline (`reports/reproducibility/00_security_readonly_check.md`), exported env-var names only (`reports/reproducibility/00_env_var_names.txt`), and the consolidated baseline report (`reports/reproducibility/00_current_state_baseline.md`). Read-only VPS revalidation in the same block confirmed the temporary authorized-key comment `openclaw-agent-7.2.6-temporal` still exists and that exposed/listening ports still include `5432`, `62880`, `62881`, `80`, `443`, and `5678`. No Traefik, systemd, firewall, Drive, or production service write was applied in this CP0 closure pass.
- On 2026-08-04 midday, a read-only production topology/parity audit was documented. Literal inspection confirmed: `/data/gdrive` root access from this runtime; readable VPS production-mounted project access at `/mnt/gdrive/Portfolio/daily-customer-churn-predictor`; readable GitHub raw access for the public repository; absence of `.git` and `.gitattributes` in both the Drive-mounted and VPS-mounted project copies; active `churn-api.service` running from `/mnt/gdrive/Portfolio/daily-customer-churn-predictor` on port `62881`; live Traefik file still routing only `Host(srv1624621.hstgr.cloud) && PathPrefix(/phase7)` to that backend; and SHA-256 parity across workspace + Drive + VPS for `README.md`, `STATUS.md`, `src/api/churn_service.py`, and `run_7_2_5a.sh`, while the sampled GitHub `main` hashes differ and `run_7_2_5a.sh` is absent there. Functional route checks also confirmed the served backend still returns `200` for legacy `/phase7/reporting` but `404` for `/customer-churn/dashboard`, `/customer-churn/tested-actions-approval`, and `/customer-churn/new-actions-testing`; public HTTPS returned `504` for `/phase7/reporting` and `404` for `/customer-churn/dashboard`. This leaves the honest production state documented as a legacy-serving backend plus legacy-only Traefik exposure, not as a completed live rollout of the new `/customer-churn/...` surface. No write/restart/proxy change was performed in that audit.
- On 2026-08-13 late night, Fase 7 reproducibility was advanced locally for the synthetic demo bundle. The legacy verdict name `insufficient_power` was normalized away in favor of the real canonical `insufficient_sample` produced by `src/pipeline/ab_testing_framework.py`, the synthetic demo builder was corrected so post-test synthetic winners remain in `stat_test_completed` until explicit business resolution, and the rebuilt synthetic review payload now returns `post_test_pending_count = 3` under `build_phase7_review_page_view_model(..., mode="synthetic_demo")`. A formal manifest plus validator were added for the canonical synthetic bundle (`data/synthetic_demo/synthetic_demo__phase7_manifest_20260727.json`, mirrored in `reports/reproducibility/PHASE7_SYNTHETIC_DEMO_MANIFEST_20260727.json`), manifest validation passed with exact hash/size matches for the 6 tracked artifacts, and isolated smoke reproducibility confirmed stable manifest hash across two consecutive synthetic rebuilds in a clean copied workspace. Notebook cleanup/smoke recommendations were also captured in `reports/reproducibility/PHASE7_NOTEBOOK_CLEANUP_PROPOSAL_20260813.md`.
- On 2026-08-04 early morning, the Phase 7 business-dashboard rebrand/split was implemented locally. The business-facing routing is now `/customer-churn/dashboard`, `/customer-churn/tested-actions-approval`, and `/customer-churn/new-actions-testing`, each with the expected public/protected behavior, while the protected write endpoints remain unchanged under `/phase7/...`. The reporting/review renderers were refactored to match the new VivaMarket business layout, the local smoke script `run_7_2_5a.sh` was updated to the new route family, and the focused regression suite `python3 -m unittest tests.test_phase7_reporting_page tests.test_phase7_review_page tests.test_churn_service -v` passed `36/36`.
- On 2026-08-01 evening, Alberto confirmed that the old OpenAI key `[REDACTED]` had already been revoked in the OpenAI dashboard. The project traceability was updated accordingly: the key remains present only as a dead legacy value in the local and Drive `.env` files, no `.env` replacement was applied yet, and this removes the last background pending item from the formal Phase 7 closure.
- In the same execution block, the business-portfolio repackaging layer requested by Alberto was completed end to end without touching production services. `README.md` was refreshed with two real captures taken from the currently running local service surfaces (`/phase7/reporting` and `/phase7/review/page-2-2?token=...`), and four new publication artifacts were generated: `reports/vivamarket_portfolio_executive_summary_20260801.html`, `reports/vivamarket_portfolio_executive_summary_20260801.pdf`, `reports/vivamarket_portfolio_business_deck_20260801.html`, and `reports/vivamarket_portfolio_business_deck_20260801.pdf`. Minimal verification passed through live screenshot generation with headless Chromium and `file` validation of the generated PDFs (`7` pages executive summary, `8` pages business deck).
- Before closing that package, the apparent public `ERR_CONNECTION_REFUSED` incident was re-investigated end to end. The conclusion is now explicit: the public service path was healthy, and the failure came from this runtime's local DNS resolution of `srv1624621.hstgr.cloud` to `127.0.1.1`. Literal public evidence was then re-collected with `curl --resolve` and stored in the incident trail: `curl -k -sS --resolve srv1624621.hstgr.cloud:443:187.127.225.147 -o /tmp/phase7_reporting_resolve_check_20260801.html -w "http_code=%{http_code} time_total=%{time_total}\n" https://srv1624621.hstgr.cloud/phase7/reporting` returned `http_code=200 time_total=2.402568`, and `curl -k -sS --resolve srv1624621.hstgr.cloud:443:187.127.225.147 -o /tmp/phase7_review_resolve_check_20260801.html -w "http_code=%{http_code} time_total=%{time_total}\n" "https://srv1624621.hstgr.cloud/phase7/review/page-2-2?token=phase7-test-token"` returned `http_code=200 time_total=1.081788`. Plain in-runtime resolution without `--resolve` still failed immediately. This was therefore an environment-local access issue, not a new Traefik or churn-api outage. The separate `Canvas: b44977ca8092 failed` message was also confirmed to be non-impacting tool noise: it did not alter any of the seven generated deliverables, and all of them remained intact and usable.
- After that, the final interview layer was added as a reusable project artifact: `reports/vivamarket_portfolio_interview_copy_20260801.md`. It includes 30-second, 60-second, and 90-second spoken versions, plus recruiter-facing, hiring-manager-facing, and technical-with-business framing so the packaging block now closes with both visual assets and ready-to-use spoken copy.
- On 2026-08-01 midday, the remaining protected-review-page cold-start incident for `7.2.6` was closed in code and revalidated live. The root cause was confirmed in the Drive-mounted production code path serving the API (`PYTHONPATH=/data/gdrive/Portfolio/daily-customer-churn-predictor/src`): page 2.2 re-read the same Drive-backed integrated-actions artifact twice per request and lacked any in-memory TTL cache, so a cold 10-way burst could stampede the mounted filesystem and hang the backend before Traefik even became relevant. The fix was implemented in `src/evidence/phase7_integration.py` and `src/api/phase7_review_page.py`, synchronized to the Drive copy, and validated through targeted regression tests plus fresh cold-start live campaigns after restarting the API before each campaign. Results: direct backend sequential `10/10`, public Traefik sequential `10/10`, public Traefik parallel `10/10`, and an additional cold direct-backend parallel check `10/10`. This closes the last real `7.2.6` blocker without new Traefik changes.
- On 2026-07-31 late morning, the last strict closure dependency for Phase 7 was cleared by **independent direct inspection of the GDrive/production copy** of `src/api/churn_service.py`. That inspection confirmed the single-business-approver token gate is enforced in the Drive-side code for both `POST /phase7/stat-launch-requests` and `POST /phase7/stat-launch-requests/execute` via `_require_phase7_review_token(self)`, matching the local workspace implementation and eliminating the remaining formal-close doubt documented in `phase7_action_plan_v41.md`. In the same closure pass, the operational Phase 7 files were re-aligned and verified between local workspace and `/data/gdrive/Portfolio/daily-customer-churn-predictor/`, including `STATUS.md`, `_private/_docs/tech_doc.md`, `src/api/churn_service.py`, `src/evidence/phase7_artifacts.py`, `tests/test_churn_service.py`, `tests/test_phase7_synthetic_demo_mode.py`, `scripts/build_phase7_synthetic_demo_bundle.py`, the canonical synthetic-demo artifacts, and the canonical `phase7_integrated_actions_20260727` JSON/report pair.
- On 2026-07-31 morning, Block 3 (`7.3.1` + `7.3.2`) was completed locally. New shared resolver module `src/evidence/phase7_artifacts.py` now makes `real` the explicit default Phase 7 mode and routes synthetic-demo reads/writes only when `MODE=synthetic_demo` (or an equivalent explicit internal call) is deliberately selected. Phase 7 artifact builders/loaders were updated to resolve their namespace explicitly instead of relying on basename-only lookup, and the synthetic-demo canonical runtime artifacts now use `synthetic_demo__` prefixes inside `data/synthetic_demo/`, including the seeded full-cycle package created by `scripts/build_phase7_synthetic_demo_bundle.py`. Dashboard pages 2.1 and 2.2 now render a fixed non-dismissible banner — `DATOS SINTÉTICOS — NO SON RESULTADOS REALES` — whenever they consume synthetic-demo artifacts. Verification passed through `python3 -m unittest tests.test_phase7_reporting_page tests.test_phase7_review_page tests.test_phase7_synthetic_demo_mode` (`10/10` passed), full shared regression `python3 -m unittest tests.test_churn_service` (`28/28` passed), and a real local synthetic bundle build via `MODE=synthetic_demo python3 scripts/build_phase7_synthetic_demo_bundle.py`, which generated the canonical prefixed synthetic integrated-actions / stat-runs / history artifacts under `data/synthetic_demo/`. Immediately after that closure pass, the relevant non-sensitive Block 3 code, docs, tests, and canonical synthetic-demo artifacts were synchronized to `/data/gdrive/Portfolio/daily-customer-churn-predictor/` and verified by exact file comparison to simplify Alberto review from Drive.
- On 2026-07-31 just after midnight, sub-phase `7.1.7` was closed at full local + Drive-verified scope. `src/api/churn_service.py` now enforces the same Phase 7 review token gate on `POST /phase7/stat-launch-requests` and `POST /phase7/stat-launch-requests/execute` that was already required on the other sensitive governance routes, removing the last live contradiction against the documented single-business-approver model. `tests/test_churn_service.py` was updated so governed launch/create flows use the Phase 7 review headers consistently, a new regression now proves both launch-request routes return `401` before persisting or executing when the token is missing/invalid, and the Phase 7 test reset path was hardened to clear prior governed-launch parquet state so the suite stays deterministic. Verification passed through `python3 -m unittest tests.test_churn_service.TestChurnService.test_phase7_review_token_rejects_launch_request_and_execute_routes tests.test_churn_service.TestChurnService.test_phase7_integrated_actions_and_stat_launch_endpoints tests.test_churn_service.TestChurnService.test_phase7_post_test_decision_endpoint tests.test_churn_service.TestChurnService.test_phase7_kpi_and_n8n_endpoints tests.test_phase7_integration -v` (`26/26` tests passed) plus a real local HTTP check showing `401` without token and `201` with token for the governed launch-request creation route, and `401` without token for the governed execute route. The corresponding non-sensitive project files for this `7.1.7` closure were then replicated to `/data/gdrive/Portfolio/daily-customer-churn-predictor/` and verified by exact file comparison.
- On 2026-07-28 early evening, Section 7 point 1 from the Phase 7 plan was implemented locally. `src/evidence/phase7_integration.py` now merges persisted integrated-action state when regenerating `phase7_integrated_actions_*.json` instead of wiping governance/stat lifecycle fields, while also preserving the decision-time business snapshot if an already-decided action’s underlying draft changes later. New artifact flags `draft_content_changed_since_decision` and `draft_refresh_blocked_reason` make that preservation explicit. In parallel, `src/api/churn_service.py` now degrades `POST /phase7/stat-tests/launch` to a `410 Gone` public response so the governed `/phase7/stat-launch-requests` flow becomes the only public HTTP launch path. Focused regression coverage was added in `tests/test_phase7_integration.py` and `tests/test_churn_service.py`; the new point-1-specific unit tests passed, and a direct live local server verification confirmed the old route now returns `410` with the deprecation payload.
- On 2026-07-28 early afternoon, the second real `7.1.6` slice was completed locally. The direct statistical-launch payload is now wrapped by a governed launch-request layer in `src/evidence/phase7_integration.py`: new functions `create_phase7_stat_launch_request()`, `execute_phase7_stat_launch_request()`, and `load_phase7_launch_requests()` persist the intermediate request artifact `data/processed/phase7_stat_launch_requests.parquet`, mark approved actions as `launch_requested`, and then execute the statistical engine from that governed request instead of depending only on an ad-hoc manual launch payload. `src/api/churn_service.py` now also exposes `POST /phase7/stat-launch-requests`, `POST /phase7/stat-launch-requests/execute`, and `GET /phase7/stat-launch-requests/latest`. Verification passed through `python3 -m unittest tests.test_phase7_integration tests.test_churn_service.TestChurnService.test_phase7_integrated_actions_and_stat_launch_endpoints tests.test_churn_service.TestChurnService.test_phase7_kpi_and_n8n_endpoints -v` (`9/9` tests passed), and a real local governed-launch smoke pass also succeeded by rebuilding `20260727`, approving `P7-ACT-004`, and persisting launch request `p7-launch-20260727-c838b418` into the canonical artifact line.
- On 2026-07-28 midday, the first real `7.1.6` lifecycle/governance slice was completed locally. `src/evidence/phase7_integration.py` now extends each integrated Phase 7 action with explicit approval + lifecycle fields (`approval_status`, `approval_required`, `launch_status`, `lifecycle_state`, decision metadata), persists append-only governance/stat history in `data/processed/phase7_action_history_log.parquet`, and requires a business approval step before statistical launch. `src/api/churn_service.py` now also exposes `POST /phase7/actions/decision` and `GET /phase7/action-history/latest`, and the canonical artifact `data/processed/phase7_integrated_actions_20260727.json` was regenerated so the current `20260727` action set now carries the new lifecycle baseline with `pending_review` as the initial governance state. Verification passed through `python3 -m unittest tests.test_phase7_integration tests.test_churn_service.TestChurnService.test_phase7_integrated_actions_and_stat_launch_endpoints tests.test_churn_service.TestChurnService.test_phase7_kpi_and_n8n_endpoints -v` (`8/8` tests passed) plus real local regeneration via `python3 scripts/build_phase7_integrated_actions.py --run-date 20260727`.
- On 2026-07-28 morning, the fourth real `7.1.5` slice was completed locally. The Phase 7 path now also exposes downstream KPI and n8n-style payload surfaces based on the persisted statistical results: `build_phase7_kpi_status_view()`, `build_phase7_n8n_payload()`, `GET /phase7/kpis/latest`, and `GET /phase7/n8n-payload/latest`, plus the new execution scripts `build_phase7_kpi_status.py` and `build_phase7_n8n_payload.py`. Verification passed through `python3 -m unittest tests.test_phase7_integration tests.test_churn_service.TestChurnService.test_phase7_integrated_actions_and_stat_launch_endpoints tests.test_churn_service.TestChurnService.test_phase7_kpi_and_n8n_endpoints -v` (`7/7` tests passed), and real local artifact generation also passed for run date `20260727`.
- On 2026-07-28 morning, the third real `7.1.5` slice was completed locally. The new Phase 7 path now also has an operational API surface: `GET /phase7/integrated-actions/latest`, `GET /phase7/stat-tests/latest`, and `POST /phase7/stat-tests/launch`. Supporting functions `load_phase7_stat_test_runs()` and `build_phase7_stat_summary()` were added in `src/evidence/phase7_integration.py`, and `src/api/churn_service.py` was hardened so tuple/array-like statistical values serialize safely in JSON. Verification passed through `python3 -m unittest tests.test_phase7_integration tests.test_churn_service.TestChurnService.test_phase7_integrated_actions_and_stat_launch_endpoints -v` (`5/5` tests passed).
- On 2026-07-28 morning, the second real `7.1.5` slice was completed locally. A new CLI entrypoint `scripts/launch_phase7_stat_test.py` now defines the current canonical evaluation contract for this stage as explicit control/variant counts at launch time, and the first persisted Phase 7 statistical run was executed successfully for `P7-ACT-001`. The run reused the validated engine via `evaluate_integrated_action()` and wrote `data/processed/phase7_stat_test_runs.parquet`, updating `data/processed/phase7_integrated_actions_20260727.json` with `latest_stat_run_id=p7-stat-20260727-0c5d0e10` and `latest_verdict=no_significant_difference`. Verification passed through `python3 -m unittest tests.test_phase7_integration -v` (`3/3` tests passed) plus the real command `python3 scripts/launch_phase7_stat_test.py --action-id P7-ACT-001 --proposal-run-date 20260727 --launched-by <project-owner-email> --control-n 1200 --control-converted 108 --control-opt-out 12 --variant-n 1200 --variant-converted 111 --variant-opt-out 12`.
- On 2026-07-28 morning, Phase 7 `7.1.5` was started with the first real integration slice between the new Phase 7 action drafts and the already validated statistical engine. New local module `src/evidence/phase7_integration.py` now converts `data/processed/phase7_action_drafts_20260727.json` into a stat-engine-ready artifact `data/processed/phase7_integrated_actions_20260727.json` plus report `reports/phase7_integrated_actions_20260727.md`, preserving draft-level governance flags (`numeric_coherence_passed`, `generation_contract_violation`) and marking each candidate as `ready` or `blocked` before any statistical evaluation. The same module also reuses `pipeline.ab_testing_framework.run_ab_test` directly through `evaluate_integrated_action()` so verdict logic is not duplicated. Verification passed through `python3 -m unittest tests.test_phase7_integration -v` (`2/2` tests passed) and a real local build via `python3 scripts/build_phase7_integrated_actions.py --run-date 20260727`, which produced `20` integrated actions.
- On 2026-07-28 early morning, the Phase 7 default OpenAI configuration authorized in `_private/phase7_action_plan_v19.md` was implemented locally in `src/evidence/phase7_proposal_builder.py`: the default model is now `gpt-5.6-luna`, the default reasoning mode is now `PHASE7_OPENAI_REASONING_EFFORT=low`, and GPT-5.6 requests now avoid sending the unsupported explicit `temperature=0` field while preserving the existing retry / contract-validation path. Regression verification passed through `python3 -m unittest tests/test_phase7_proposal_builder.py -v` (`6/6` tests passed).
- In the same execution block, the first real full-pipeline verification run required by v19 was completed locally through `python3 scripts/build_phase7_action_drafts.py` against the real snapshot `data/processed/phase7_source_snapshot_20260727.json`. The run finished successfully and rewrote `data/processed/phase7_action_drafts_20260727.json` plus `reports/phase7_action_drafts_20260727.md` with `20/20` validated drafts, `0` `generation_contract_violation` rows, `0` `llm_retry_used` rows, and real non-empty `pros` / `cons` lists present in all `20` generated drafts. The resulting artifacts now show genuine OpenAI-backed pros/contras generation in production mode using `gpt-5.6-luna` from the project key, which operationally closes the previously missing proof for `7.1.3-bis` and removes the last blocker before `7.1.5`.
- On 2026-07-27, Alberto-provided external `MODE=synthetic_demo` draft artifacts were installed under `data/synthetic_demo/` in both the canonical local workspace and the mounted Drive copy: `phase7_source_snapshot_20260727.json`, `phase7_action_drafts_20260727.json`, `phase6_kpi_status_20260727.json`, and `README_synthetic_demo.md`. These four files are explicitly recorded as an ahead-of-time external draft package only; they do **not** close Block 4, because `phase7_action_plan_v12.md` still requires Block 1 and Block 2 to be completed first, and neither prerequisite is closed today. This package also does **not** prove three still-pending code safeguards: (a) the guard that prevents `MODE=synthetic_demo` from becoming the default mode, (b) the isolation test showing that no real execution can read/write the `data/synthetic_demo/` namespace by accident, and (c) the fixed non-dismissible dashboard banner. Those three items remain unverified until confirmed in the real code / dashboard paths.
- On 2026-07-27, a blocking verification pass for `7.1.3-bis` was executed locally against the current Phase 7 code and the Drive-delivered `phase7_action_plan_v12.md` requirements. `src/evidence/phase7_proposal_builder.py` now includes a real OpenAI-backed synthesis path (`resolve_openai_runtime_config()` + `synthesize_article_with_openai()`) that replaces the old pure `title + summary` concatenation path for business drafting, and `tests/test_phase7_proposal_builder.py` was updated accordingly. Real verification produced a mixed result: the project successfully resolved an OpenAI key from `/data/gdrive/Portfolio/daily-customer-churn-predictor/.env` and the new test suite passed (`3/3`), but both the minimal live API ping and the first real run over `data/processed/phase7_source_snapshot_20260727.json` failed with the same OpenAI HTTP `429` / `insufficient_quota` response. Therefore `7.1.3-bis` is now code-wired locally but still blocked operationally by API quota, and no real LLM-generated Phase 7 pros/contras artifact has been produced yet.
- Earlier the same day, `_private/phase7_action_plan_v10.md` was created as an implementation-audit update over the v9 baseline after Alberto ordered Plan 7 execution to stop pending sensitive improvements. The new v10 document now maps the real local implementation of Sub-phases `7.1.1`–`7.1.4` to the original roadmap text, explicitly distinguishing what is fully implemented, what is only partially implemented, and what is still missing. In particular, the audit now records that `7.1.3` currently generates deterministic article drafts from RSS snapshot content, but does **not** generate explicit `pros/contras` anywhere in the pipeline yet.
- Earlier the same day, Phase 7 execution advanced locally through Sub-phases 7.1.1–7.1.4 from the Drive-delivered `_private/phase7_action_plan_v9.md` baseline. The workspace now includes: (a) the extended source-governance schema in `config/evidence_sources_allowlist.yaml` plus persistent registry artifact `data/interim/phase7_source_scraping_registry.json`; (b) the RSS-only extractor slice in `src/evidence/phase7_extractor.py` + `scripts/build_phase7_source_snapshot.py`, which fetched the live official feeds of NNGroup and Baymard and produced `data/processed/phase7_source_snapshot_20260727.json` with `20` in-scope articles while recording the other `8` shortlist sources as out-of-scope/manual-only; and (c) the separated drafting + numeric-coherence slice in `src/evidence/phase7_proposal_builder.py` + `scripts/build_phase7_action_drafts.py`, which turned that snapshot into `data/processed/phase7_action_drafts_20260727.json` and `reports/phase7_action_drafts_20260727.md` with `20` draft proposals and `20/20` numeric-coherence validations passed. Validation passed through `python3 -m unittest tests.test_phase6_catalog_builder tests.test_phase7_extractor tests.test_phase7_proposal_builder -v` (`8/8` tests passed).
- On 2026-07-24, Sub-phase 6.5 was closed locally: `README.md` was updated with the explicit `Phase 6 — Dynamic Evidence System` closure section, the historically ambiguous recommendations artifact was renamed from `reports/phase6_intervention_recommendations_20260718.md` to `reports/phase5_step6_intervention_recommendations_20260718.md`, and the dated sign-off file `reports/phase6_case_study_signoff_20260724.md` was created. Verification passed through direct README/sign-off inspection plus a repo-wide reference check confirming the renamed Phase 5 step-6 artifact is now the referenced canonical path.
- On 2026-07-24, the first executable 6.4 integration slice was implemented locally after syncing in `phase6_action_plan_v9.md`: `src/evidence/phase6_integration.py` now builds approval-gate proposal artifacts from the latest eligible recommendations, `scripts/build_phase6_action_proposals.py` now materializes that slice for a dated run, and `src/api/churn_service.py` now exposes `/phase6/proposals/latest`, `/phase6/proposals/decision`, and `/phase6/action-history/latest`. Validation passed through `python3 -m unittest tests.test_phase6_integration tests.test_churn_service.TestChurnService.test_phase6_proposals_endpoint_and_decision_history -v` and through real execution of `python3 scripts/build_phase6_action_proposals.py --run-date 20260724`, which generated `data/processed/phase6_action_proposals_20260724.json` and `reports/phase6_action_proposals_summary_20260724.md` with one approval-gate proposal under the current seed baseline.
- On 2026-07-24, the corrected planning document was promoted to `_private/phase6_action_plan_v8.md` so the operational reference now captures the full validated state of the hardening cycle, including the later fallback fix and the deterministic test correction.
- On 2026-07-24, a follow-up fix was applied to the new Phase 6 P3.3 catalog-resolution logic after Alberto detected a real fallback bug. `src/evidence/recommendation_builder.py` now uses the latest available catalog when recommendations are requested without `--run-date`, derives the effective report date from the resolved snapshot, and no longer forces today's date into that path. `tests/test_evidence_sourcing.py` was extended with the exact no-`run_date` scenario, and validation passed through `python3 -m unittest tests.test_evidence_sourcing -v` plus a real isolated execution where only `evidence_catalog_20260710.json` existed and `python3 scripts/build_phase6_recommendations.py` correctly generated `phase6_dynamic_evidence_recommendations_20260710.md`.
- Later the same day, Alberto detected that the first regression test for that fallback fix was still clock-sensitive because it used a snapshot date that coincided with the real system day. The test has now been corrected to use fixed historical dates (`20240101` / `20240601`) and to assert the dated output filename as well, so the fallback coverage is now deterministic rather than dependent on the current date.
- On 2026-07-24, the first Phase 6 v6 hardening block was implemented locally across the current baseline artifacts: `src/evidence/catalog_builder.py`, `src/evidence/recommendation_builder.py`, `scripts/build_phase6_catalog.py`, `scripts/build_phase6_recommendations.py`, `tests/test_phase6_catalog_builder.py`, `tests/test_evidence_sourcing.py`, and `reports/phase6_validation_checklist_20260721.md`. Validation passed through `python3 -m unittest tests.test_phase6_catalog_builder tests.test_evidence_sourcing -v`, through successful dated execution of `python3 scripts/build_phase6_catalog.py --run-date 20260724` and `python3 scripts/build_phase6_recommendations.py --run-date 20260724`, and through an explicit overwrite-protection check where a second catalog build for `20260724` failed as intended with `FileExistsError`.
- On 2026-07-24, before implementing further Phase 6 improvements, the action plan was corrected and promoted to `_private/phase6_action_plan_v6.md` so the next execution block is explicitly re-sequenced around baseline hardening first: spec-compliance fix for snapshot overwrite protection, missing validation coverage, and robustness improvements within the already approved 6.1/6.2/6.3 scope. This update intentionally freezes further Phase 6 functional expansion until that hardening pass is complete.

- On 2026-07-22 just after midnight, the next Phase 6 prototype layer was completed locally: `src/evidence/recommendation_builder.py` and `scripts/build_phase6_recommendations.py` now transform the immutable seed catalog into `reports/phase6_dynamic_evidence_recommendations_20260721.md`, while `tests/test_evidence_sourcing.py` and `reports/phase6_validation_checklist_20260721.md` establish the first formal validation baseline for Sub-phase 6.3. Real execution validation passed through `python3 scripts/build_phase6_recommendations.py` and `python3 -m unittest tests.test_evidence_sourcing tests.test_phase6_catalog_builder -v`.
- On 2026-07-21 evening, Phase 6 execution formally started with the first local implementation slice. New artifacts now in place are: `docs/phase6_spec_dynamic_evidence_system.md`, `scripts/build_phase6_catalog.py`, `src/evidence/catalog_builder.py`, `tests/test_phase6_catalog_builder.py`, `data/processed/evidence_catalog_20260721.json`, and `reports/phase6_evidence_catalog_summary_20260721.md`. Real execution validation passed through `python3 scripts/build_phase6_catalog.py` and `python3 -m unittest tests.test_phase6_catalog_builder -v`, confirming that the approved seed baseline (`INT-01`, `INT-02`, `INT-04`) is now generated as an immutable catalog snapshot under the Phase 6 governance constraints.
- On 2026-06-19 late morning, a dedicated n8n-ready Phase 5 shadow workflow export was created at `n8n/n8n_workflow_phase5_shadow_loop.json`. The workflow is defined with: daily `08:00` Schedule Trigger, POST to `http://localhost:62881/agent/decisions/shadow/run`, freshness verification via `GET /agent/status/daily?refresh=true`, and Telegram Bot API alerts (through n8n variables `TELEGRAM_BOT_TOKEN` / `TELEGRAM_CHAT_ID`) for either run failure or missing same-day `agent_decision_log` refresh. In the same execution block, the shadow API was manually invoked once with `cycle_date=2026-06-19`, and the canonical local DB now verifiably contains the new record `755d5bee-9037-43a9-bfda-ef24f6a3fc98` for `2026-06-19`, moving the honest baseline from `4/14` to `5/14`.
- On 2026-06-19 morning, a new control checkpoint was implemented for Phase 5 traceability hardening: `src/pipeline/phase5_daily_status.py` now exposes source-integrity fields (canonical DB check, temp/test DB detection, latest-cycle gap, stale-log alert), and new script `src/pipeline/phase5_control_check.py` now renders `reports/phase5_control_check_latest.md` against the canonical local DB and fails loudly when the shadow log is stale or the source DB is not canonical. Real execution on the canonical DB confirmed the current incident state honestly: `4/14` shadow days, canonical DB in use, but `11` days since the latest logged cycle and therefore `attention_required` instead of silent progress.
- On 2026-06-08 morning, a consolidated continuity summary was written to `_private/phase5_execution_position_20260608.md` so the project now has one explicit document covering where Phase 5 stands, what the target is, why the hard milestones cannot advance further right now, and what still remains to be done. This freezes the current truthful state: strong structural preparation, open `4/14` shadow gate dependency, and remaining work concentrated in new valid shadow-day accumulation plus later evidence-quality completion.
- On 2026-06-08 morning, the Phase 5 operational surface gained a fully aggregated API view: `src/api/churn_service.py` now exposes `GET /agent/status/phase5` with optional `refresh=true`, returning the base `agent_status` plus the current daily-status layer and shadow-monitor layer in one payload. `tests/test_churn_service.py` was extended to validate the new aggregate endpoint, and the combined regression suite now passes with `19` tests. This removes another manual integration step for any future monitoring/orchestration that needs the whole current Phase 5 state at once.
- On 2026-06-08 morning, the shadow-monitor layer was promoted from file/report surface to API-queryable operational surface as well. `src/api/churn_service.py` now exposes `GET /agent/status/shadow-monitor` with optional `refresh=true`, returning the live shadow-monitor payload plus artifact paths, and `tests/test_churn_service.py` was extended to validate that endpoint. The combined regression suite now passes with `18` tests. This makes both major derived Phase 5 status layers — daily summary and shadow monitor — queryable through the service API itself.
- On 2026-06-08 morning, the remaining manual-shadow logging gap was also closed: `POST /agent/decisions/shadow` now refreshes the same derived Phase 5 artifacts as the automatic shadow-run path, including the synchronized daily-status refresh metadata. This means any new shadow decision entry — manual create, automatic run, or reconciliation — now keeps the shadow monitor and daily status surfaces aligned through one shared refresh flow. Regression coverage in `tests/test_churn_service.py` was extended accordingly and the combined suite passed successfully (`17` tests).
- On 2026-06-08 morning, the Phase 5 status surfaces were synchronized more tightly: whenever shadow-run or shadow-reconcile refreshes the shadow monitor artifacts, the daily-status artifacts are now refreshed automatically as part of the same API-side refresh flow. `src/api/churn_service.py` now regenerates the daily JSON/markdown status snapshot inside the shadow refresh helper and returns that result inside `monitor_refresh.daily_status_refresh`, while regression coverage in `tests/test_churn_service.py` was extended and the combined suite passed successfully (`17` tests). This closes a real operational gap: the latest Phase 5 status no longer depends on remembering a separate refresh step after shadow-log changes.
- On 2026-06-08 morning, the new Phase 5 daily-status layer was promoted from script-only utility to API-accessible operational surface. `src/api/churn_service.py` now exposes `GET /agent/status/daily` with optional `refresh=true`, returning the current gate snapshot, decision-quality state, operational status, latest cycle review, artifact paths, and a markdown preview derived from the live shadow evidence. Coverage in `tests/test_churn_service.py` was extended accordingly, and the combined regression suite passed successfully (`17` tests). This makes the current Phase 5 status queryable through the service itself instead of only through generated files.
- On 2026-06-08 morning, the Phase 5 regression harness was cleaned up further by adding explicit test teardown/disposal for the temporary SQLite-backed suites (`tests/test_phase5_daily_status.py`, `tests/test_phase5_shadow_monitor.py`, `tests/test_churn_service.py`). The combined warning-enabled suite still passes (`16` tests), and the noisiest temporary-directory cleanup warnings were removed, leaving only lower-level residual SQLite `ResourceWarning` noise during some library paths. This means the current hardening frontier is now moving from clearly useful cleanup into diminishing-return noise reduction.
- On 2026-06-08 morning, the new daily Phase 5 status layer was hardened with regression coverage. New test module `tests/test_phase5_daily_status.py` now validates the generated gate snapshot and markdown rendering, and the combined regression suite `python3 -m unittest tests.test_phase5_daily_status tests.test_phase5_shadow_monitor tests.test_churn_service -v` passed successfully (`16` tests). This means the new status generator is no longer just useful manually; it is now part of the tested Phase 5 operational surface.
- On 2026-06-08 morning, the first live-derived daily Phase 5 status generator was implemented as `src/pipeline/phase5_daily_status.py`. The new script now converts the current shadow-monitor evidence into two reusable artifacts — `data/processed/phase5_daily_status_latest.json` and `reports/phase5_daily_status_latest.md` — so the project has an explicit machine-readable + human-readable snapshot of gate progress, decision-quality state, operational status, latest cycle review, and next honest step. Real execution confirmed the current snapshot as `4/14` shadow days, `4/4` matches, `0` divergences, `0` pending, and `dispatch_confirm` as the current recommended routine state.
- On 2026-06-08 morning, a new private daily-traceability artifact `_private/phase5_daily_progress_template.md` was created to help separate true evidence movement from documentary-only movement during the Phase 5 waiting window. The template defines a compact daily entry format covering execution state, real evidence progress, documentary/methodological progress, current interpretation, and next honest step, so future progress summaries stay clearer while the hard gate remains time-bound.
- On 2026-06-08 morning, a new private gate-logic artifact `_private/phase5_decision_gate_matrix.md` was created to make the correct continue / pause / escalate response explicit across common Phase 5 states. The matrix now clarifies which situations should permit ongoing parallel work with caveats, which should pause interpretation or publication wording, and which should escalate directly to Alberto, while preserving the current truthful reading that Block G remains open and the default execution mode is still `continue with caveat` rather than gate closure.
- On 2026-06-08 morning, a new private analytical artifact `_private/phase5_dimension_readiness_matrix.md` was created to make Block I readiness explicit by dimension instead of treating Phase 5 comparison readiness as one undifferentiated state. The matrix now defines when each of the four comparison dimensions (business KPI, response speed, decision quality, operational cost) should be treated as not ready, partial, or strong, and it records the current truthful position: only decision quality is partially usable, while the other three dimensions remain not ready.
- On 2026-06-08 morning, a new private interpretation guardrail `_private/phase5_claim_strength_guide.md` was created so later Block I / Block J conclusions cannot quietly become stronger than the evidence allows. The guide defines claim-strength levels, safe-vs-unsafe wording, dimension-specific guardrails, the currently allowed language at the present `4/14` routine-shadow state, and the upgrade conditions required before stronger comparative claims become honest.
- On 2026-06-08 morning, a new private publication-quality artifact `_private/phase5_narrative_coherence_checklist.md` was created so the later Block J coherence review can be executed as an explicit checklist instead of an ad hoc editorial pass. It now freezes the consistency checks for scope, terminology, evidence, limitations, recommendation strength, tone, and visual/reporting coherence, plus failure signals that would block an honest Phase 5 publication pass.
- On 2026-06-08 morning, a new private execution-traceability artifact `_private/phase5_gap_closure_tracker.md` was created to turn the known Phase 5 evidence gaps into explicit closure conditions. The tracker now states what must become true before each currently partial dimension can be treated as strong evidence, especially for the hard shadow gate (`4/14` → `14/14`), timestamp completeness, human-hours completeness, anomaly diversity, and future live override evidence.
- On 2026-06-08 morning, a new private publication-preparation artifact `_private/phase5_publication_outline.md` was created so the future Block J narrative does not start from a blank page once the evidence is ready. The outline freezes the intended publication package, core story spine, draftable-vs-blocked sections, recommended case-study structure, README Phase 5 skeleton, and current narrative risk (premature conclusion pressure while the final comparison is still incomplete).
- On 2026-06-08 morning, a new private traceability artifact `_private/phase5_evidence_map.md` was created so later Phase 5 publication claims can be tied explicitly to concrete supporting artifacts instead of being reconstructed ad hoc at release time. The map links each major Block G/H/I/J claim or gate to its primary evidence, current status, and main limitation, and it makes the remaining high-value evidence gaps explicit (`14/14` shadow days, timestamp completeness, human-hours completeness, and anomaly diversity).
- On 2026-06-08 morning, a new private methodological artifact `_private/phase5_confound_register.md` was created to support the user-approved “mode 2” parallel advance while Block G continues waiting on calendar-valid shadow days. The register freezes the main Block I comparison confounds (sequential-period design, synthetic/demo boundary, human-hours completeness risk, response-speed field asymmetry, low anomaly diversity, and related interpretation limits) so later Phase 5 reporting does not over-claim beyond the evidence.
- On 2026-06-08 morning, a new private operational readiness artifact `_private/phase5_live_harness_protocol.md` was created so later Block H activation does not depend on improvisation. The protocol freezes a concrete pre-activation checklist, canonical escalation message format, response-speed timestamp discipline, human-override handling, human-hours logging guide, and per-cycle evidence checklist, all aligned with the current `4/14` Block G state and the frozen Phase 5 methodology.
- On 2026-06-08 morning, the executable Block I notebook was materially strengthened beyond the initial scaffold: `notebooks/10_phase5_autonomous_agent_ab_analysis.ipynb` now reads the real `kpi_payload` structure from `phase4_governance_monitor_latest.json` instead of showing empty KPI placeholders, surfaces current shadow-gate completion explicitly (`4/14` cycles), adds per-cycle decision-quality trend visibility, checks timestamp readiness for future response-speed analysis, and quantifies the current human-hours logging completeness gap against observed shadow cycles. The notebook was re-executed successfully through `nbclient` after the upgrade, so the saved artifact now contains a more truthful readiness baseline for Block I while Block G continues accumulating days.
- On 2026-06-08 overnight, the next non-blocked Phase 5 workstream was advanced in parallel to Block G accumulation so the project does not wait idly for calendar days: new notebook `notebooks/10_phase5_autonomous_agent_ab_analysis.ipynb` was created as the first executable Block I analysis scaffold, following the project chapter/narrative rules and covering the six planned sections (experimental framing, business KPIs, response speed, decision quality, operational cost, and recommendation). In the same pass, `_private/human_hours_log.csv` was created as the Phase 5 cost-logging template because that evidence cannot be reconstructed reliably later. The notebook was executed successfully through a real run with `nbclient`, so the current saved artifact already loads the live shadow log, the Phase 4 governance baseline, and the human-hours template without runtime errors.
- On 2026-06-08 early morning, Block G advanced again with real execution evidence instead of only instrumentation: the missing `2026-06-07` shadow cycle was logged, the pending `2026-06-05` / `2026-06-06` rows were reconciled, and the current local shadow sample now stands at `4` logged cycles, `4` reconciled cycles, `4` matches, `0` pending human decisions, and `0` divergences. During that pass, the live API on port `62881` was also reloaded so `POST /agent/decisions/shadow/reconcile` again exposes the expected `monitor_refresh` metadata from the latest Block G code, and `src/pipeline/phase5_shadow_monitor.py` was tightened so the report now distinguishes the latest cycle recorded in `agent_decision_log` from the older operational source-cycle field shown by `/agent/status`. Regression validation passed again through `python3 -m unittest tests.test_phase5_shadow_monitor tests.test_churn_service -v`, and the refreshed monitor artifacts were regenerated successfully.
- On 2026-06-07, the publication surface was updated and synchronized again using Alberto-provided artifacts: the project `README.md` was replaced with the corrected portfolio version that explicitly references `reports/vivamarket_churn_business_deck_20260531.pptx`, and the missing business deck PPTX was then copied into `reports/` locally and into the mounted Drive project copy after it proved unavailable through the Drive mount listing alone. Both artifacts are now present in the local workspace and verified in Drive.
- On 2026-06-07, the next Block G automation step was implemented locally: `src/api/churn_service.py` now refreshes `reports/phase5_shadow_monitor_latest.html` and `reports/phase5_shadow_divergences.md` automatically after both `POST /agent/decisions/shadow/run` and `POST /agent/decisions/shadow/reconcile`, returning `monitor_refresh` metadata in the API response so the reporting surface stays synchronized with the shadow log without a manual reporting command. Coverage in `tests/test_churn_service.py` was extended for the new refresh hook, `tests/test_phase5_shadow_monitor.py` was stabilized for shared-suite execution, and the combined API/reporting regression suite passed successfully.
- On 2026-06-07, the Block G shadow monitoring surface was strengthened locally beyond the first reporting baseline: `src/pipeline/phase5_shadow_monitor.py` now also classifies reconciled divergences as `critical` vs `non-critical` according to `reports/phase5_behavior_spec.md`, exposes a per-cycle reconciliation trend table, and surfaces `critical_divergence_cycles` directly in both `reports/phase5_shadow_monitor_latest.html` and `reports/phase5_shadow_divergences.md`. Regression coverage in `tests/test_phase5_shadow_monitor.py` was expanded accordingly, and real execution regenerated the refreshed artifacts successfully against the live local `agent_decision_log` baseline.
- On 2026-06-07, the next agreed Block G reporting step was implemented locally: new pipeline module `src/pipeline/phase5_shadow_monitor.py` now builds `reports/phase5_shadow_monitor_latest.html` plus `reports/phase5_shadow_divergences.md`, turning the shadow log into an operational monitoring surface with match-rate summary, pending-reconciliation visibility, current trigger snapshot, recent-cycle review, and explicit divergence extraction without manual database inspection. Regression coverage was added in `tests/test_phase5_shadow_monitor.py`, and real execution regenerated the new artifacts successfully against the live local `agent_decision_log` baseline.
- On 2026-06-05, Block G closed the next evaluation gap after automatic shadow generation: `src/api/churn_service.py` now also exposes `POST /agent/decisions/shadow/reconcile`, same-day `human_decision` can now be attached to a shadow record by `record_id` or `cycle_date`, `match` is computed automatically, and the live local API successfully ran + reconciled a `2026-06-08` shadow cycle with `match=true` while `/agent/status` now reports `shadow_log_count`, `matched_shadow_count`, and `pending_human_decision_count`.
- On 2026-06-05, Alberto explicitly approved `reports/phase5_behavior_spec.md`, so the document is now frozen as the operative Block G behavior contract and Pre-G can be treated as closed for the current Phase 5 baseline.
- On 2026-06-05, the Pre-G closeout advanced again beyond the normalization decisions: `SOUL.md` was updated with the project-specific Phase 5 guardrail so the Daily Customer Churn Predictor now explicitly references the behavior taxonomy, requires `analytics-reporting-ux` before new reporting/dashboard redesigns, requires `assets/images/visual_identity_guide_v1.pdf` for new HTML/interface work, and defaults ambiguous states to escalation.
- On 2026-06-05, Alberto closed the remaining Pre-G normalization decision for run/version semantics: `run_id` and `model_version` must now be normalized to the frozen `20260506` comparison family for Pre-G use, while the original publication values remain only as lineage metadata outside the normalized comparison interpretation.
- On 2026-06-05, Alberto resolved the next Pre-G normalization decisions for the publication comparison surface and the verification report was updated accordingly: Option A was approved for `campaign_cycle` (`snapshot_date` is now the accepted anchor), and Option C was approved for `action_ts` (kept explicitly unavailable and documented as a baseline limitation rather than backfilled from a weak proxy).
- On 2026-06-05, the Pre-G publication-surface schema alignment was advanced with direct parquet evidence inside `reports/phase5_pre_block_g_verification.md`: `retention_actions_20260519.parquet` was checked field-by-field against the expected Phase 5 surface, confirming direct mappings for `score`, `tier`, and `holdout`, a derivation gap for `campaign_cycle`, a still-missing `action_ts`, and a semantic mismatch on `run_id` / `model_version` versus the frozen `20260506` comparison family.
- On 2026-06-05, the runtime verification for Option A was completed end-to-end: `scripts/ensure_churn_api_62881.sh` now exports `CHURN_SCORING_BUNDLE` pointing to the canonical `20260506` bundle, the API process was restarted under that configuration, and `/health` now confirms that the service is actually serving `run_id=canonical_v2c_20260506` from `churn_scoring_package_20260506.joblib`.
- On 2026-06-04, the missing canonical bundle was reconstructed as `models/churn_scoring_package_20260506.joblib` and verified to expose the intended baseline metadata (`run_id=canonical_v2c_20260506`, `run_date_tag=20260506`, `model_version=V2C_20260506`). The reconstruction is explicitly traceable inside the bundle metadata as a restoration artifact derived from surviving canonical `20260506` prediction/feature artifacts plus the preserved `20260519` deployment bundle.
- On 2026-06-04, the API-loading logic was hardened toward the user-approved Option A baseline policy: `src/api/churn_service.py` now supports explicit scoring-bundle pinning through `CHURN_SCORING_BUNDLE` instead of relying only on `latest file wins`, and `.env.example` was extended to document that reproducibility control. This reduces serving ambiguity, but the canonical `churn_scoring_package_20260506.joblib` artifact is still physically absent, so the baseline-alignment blocker is now narrowed to artifact restoration / reconstruction rather than loader policy ambiguity.
- On 2026-06-04, `reports/phase5_pre_block_g_verification.md` was created and partially verified with real artifact evidence. It confirms that the synthetic Phase 4 measurement layer is internally coherent (FK integrity and holdout stability pass), but surfaces a material Pre-G inconsistency: the Phase 5 plan freezes the canonical model at `20260506`, while `src/api/churn_service.py` currently resolves the latest available bundle dynamically and the only bundle present in `models/` is `churn_scoring_package_20260519.joblib`.
- On 2026-06-04, `reports/phase5_behavior_spec.md` was created as the first Pre-G execution artifact. It formalizes the expected agent-behavior contract for shadow-mode evaluation: scenario table, decision taxonomy reference, critical-divergence definition, shadow-period evaluation rule, and prospective-only change control.
- On 2026-06-04, the private Phase 5 plan was promoted to `_private/PHASE5_action_plan_v4.md` and the previous Phase 5 action-plan file was removed so the workspace now keeps only the new canonical version. The v4 file preserves the verified OSWorld benchmark anchor (roughly **12% → 66.3%**, within **6 percentage points of human performance**) and intentionally excludes other previously discussed benchmark figures that were not re-verified with sufficient confidence.
- On 2026-06-04, `_private/PHASE5_action_plan_v3.md` was created as the new operative Phase 5 planning baseline. It keeps the bounded-autonomy / Human-on-the-Loop architecture from v2 but strengthens the methodology explicitly: the comparison is now framed as a **quasi-experimental before/after controlled comparison** rather than a classical A/B test; `ground truth` for decision quality is frozen and defined against the Pre-G taxonomy; `response speed` now has a concrete timestamp protocol; `human-hours` now has a formal measurement protocol and a ≥90% logging-completeness rule for strong claims; and `critical divergence` in Block G is operationally defined.
- On 2026-06-03, the README executive framing was rewritten to improve self-contained portfolio persuasiveness: the opening now leads with the project as a portfolio-grade end-to-end churn intelligence system built on real ecommerce data, reduces defensive repetition around the demo boundary, and reframes the Phase 4 scope as a deliberate evaluation-boundary choice rather than as a perceived weakness.
- On 2026-05-31, the README `File Structure` section was refined for GitHub readability: canonical notebooks were listed explicitly, the `src/` layout was opened to the key API/model/pipeline files, and only the active/canonical `reports/` artifacts were kept visible in the public tree.
- On 2026-05-31, `reports/archive/` was explicitly retained as a private/local historical traceability folder and added to `.gitignore` so it stays out of the public GitHub package while remaining available locally and in Drive.
- On 2026-05-31, the stakeholder-facing explainability surface was rebuilt to remove the illegible static force-view section, replace it with audience-friendly explanatory narratives, and add consistent VivaMarket logo branding across the remaining active reports that still lacked it (`churn_explainability_20260519.html`, `model_diagnostics_20260519.html`, `n8n_orchestration_20260519.html`, `roi_simulation_20260519.html`, plus active markdown report headers).
- On 2026-05-31, the report/publication surface was tightened again for GitHub readiness: the README status badge was removed, `reports/model_card_v2_20260519.md` and `reports/churn_monitoring_dashboard_20260519.html` were moved to `reports/archive/` as historical-only artifacts, the internal-vs-stakeholder split between `phase4_campaign_kpi_monitor_latest.html` and `phase4_bi_dashboard_demo.html` was made explicit, and the active Phase 4 reports were refreshed so internal honesty labels remain only in the technical monitors.
- On 2026-05-31, `README.md` was adjusted again so the executive framing and results sections present the current improved metrics directly, while version-to-version comparisons remain only in `Project Evolution`.
- v1.0.0 is considered published to GitHub by the user after the README, release notes, branding, notebook normalization, and reporting refactor.
- Phase 2 / v2 was specified operationally in the project documentation as a true analytical redesign, not a silent continuation of v1.
- `NB03` was updated and executed with the selected canonical `V2C` formulation:
  - `total_orders >= 2`
  - `tenure_days >= 90`
  - adaptive horizon `1.25x` median gap bounded to `75-150` days
  - fallback horizon `75` days
- The canonical v2 feature base was regenerated locally as `data/processed/churn_features_20260506.parquet`.
- `NB04` was re-executed on that canonical `V2C` base.
- `NB05` was adapted to the canonical `V2C` line, debugged through multiple execution fixes, and executed successfully.
- New diagnostics artifacts produced locally:
  - `data/processed/churn_diagnostics_20260506.csv`
  - `reports/model_diagnostics_20260506.html`
- `NB06` was adapted to the canonical `V2C` line and executed successfully.
- New explainability artifacts produced locally:
  - `data/processed/churn_explainability_20260506.parquet`
  - `data/processed/churn_driver_summary_20260506.csv`
  - `reports/churn_explainability_20260506.html`
- `NB07` was adapted to the canonical `V2C` line and executed successfully.
- New deployment-preparation artifacts produced locally:
  - `models/churn_scoring_package_20260506.joblib`
  - `src/models/churn_scoring.py`
  - `data/processed/churn_inference_smoke_test_20260506.parquet`
- `NB08` was adapted to the canonical `V2C` line and executed successfully.
- New orchestration artifacts produced locally:
  - `data/processed/retention_actions_20260506.parquet`
  - `n8n/daily_churn_retention_workflow_20260506.json`
  - `reports/n8n_orchestration_20260506.html`
- `NB09` was adapted to the canonical `V2C` line and executed successfully.
- New reporting artifact produced locally:
  - `reports/churn_monitoring_dashboard_20260506.html`
- Publication-layer hardening was then completed locally:
  - `README.md` rewritten to reflect the canonical V2C candidate state instead of the old v1-only framing
  - critical narrative sections later restored into `README.md` as requested (expanded executive framing, methodology, architecture, file structure, technical stack, methodological notes, roadmap, and contact/license section)
  - `requirements.txt` aligned with actual project dependencies (`shap`, `joblib`, `pyarrow`, `jupyterlab`)
  - `Dockerfile` created for reproducible containerized notebook execution
  - `.dockerignore` created to keep the image context clean
  - lightweight contract tests added in `tests/test_churn_scoring.py`
  - `RELEASE_NOTES_v1.md` extended with a clear local post-v1 hardening note
- The requested asset replacement has now been completed:
  - `assets/images/logo.png` was removed from the project
  - `assets/images/logo.gif` was added from the approved Drive/workspace source
  - remaining branded HTML exports that still referenced `logo.png` were repointed to `logo.gif`
- Local publication validation executed successfully:
  - `python3 -m py_compile src/models/churn_scoring.py`
  - `python3 -m unittest tests.test_churn_scoring -v`
- The project workspace and Drive copy were re-synchronized and verified after the publication hardening and logo replacement updates.
- A final publication sanity pass then re-confirmed:
  - `python3 -m py_compile src/models/churn_scoring.py`
  - `python3 -m unittest tests.test_churn_scoring -v`
  - remote `assets/images/` now contains `logo.gif` and `visual_identity_guide_v1.pdf`, with `logo.png` absent both locally and in Drive.
- The short controlled benchmark artifact remains available locally as `data/processed/churn_variant_benchmark_20260506.csv`.
- New local-only Phase 2 / Phase 3 reporting artifacts were produced on 2026-05-12:
  - `reports/churn_monitoring_dashboard_20260512.html`
  - `reports/model_card_v2_20260512.md`
  - `reports/roi_simulation_20260512.html`
- `notebooks/09_reporting_dashboard.ipynb` was rewritten from the earlier 8-cell lightweight version into a richer Plotly-based stakeholder dashboard notebook and executed successfully in the local workspace.
- `README.md` was expanded again to add the missing `Project Evolution` section and to list the new reporting/model-card/ROI artifacts.
- On 2026-05-13, the project README was replaced in the local workspace with the user-supplied revised version, and the visual workflow asset `assets/images/n8n_workflow_phase2.png` was added for README embedding.
- Current canonical v2 modeling artifacts produced locally:
  - `models/churn_model_20260506.joblib`
  - `data/processed/churn_predictions_20260506.parquet`
  - `data/processed/churn_model_metrics_20260506.csv`
  - `data/processed/churn_model_comparison_20260506.csv`
- On 2026-05-19, the duplicate local project folder outside `projects/TFM/` was removed after verifying that the `projects/TFM/daily-customer-churn-predictor` path is the operational canonical root used by scripts, notebook path resolution, environment assets, and project traceability.
- On 2026-05-19, the user-provided refreshed notebooks plus the updated `README.md` and `n8n/` workflow JSON files were installed into the canonical local project tree.
- Also on 2026-05-19, Phase 2 of the improvement plan was implemented and executed successfully on the canonical local notebook chain:
  - `NB05` now exports a stronger diagnostics HTML with interactive Plotly sections for risk mix and threshold precision/recall trade-offs, plus a reporting footer with run date, model version, and pipeline tag.
  - `NB06` now exports an upgraded explainability HTML with static representative HIGH/LOW SHAP force views, adjusted report styling, and the same footer metadata.
  - `NB07` now writes `model_version` and `pipeline_tag` into the scoring package metadata.
  - `NB08` was then realigned with the updated user-provided n8n workflow JSON files and re-executed so the dated orchestration HTML / JSON snapshots now match the current two-workflow architecture.
  - `NB09` now exports a richer monitoring dashboard with KPI cards, interactive Plotly visuals for risk mix / driver mix / threshold curve, and footer metadata.
  - Real execution QA was completed for `NB05`, `NB06`, `NB07`, `NB08`, and `NB09`, producing fresh 20260519 artifacts without blocking errors.
- Still on 2026-05-19, the minimum viable Phase 3 publication layer was strengthened locally after fresh execution evidence:
  - `reports/model_card_v2_20260519.md` was generated to replace the older lighter model-card narrative with a stronger governance-oriented card tied to the latest local diagnostics and payload profile.
  - `reports/roi_simulation_20260519.html` was generated to replace the older single-scenario ROI narrative with a clearer three-scenario stakeholder view (Conservative / Base case / Upside), explicit assumption disclosure, and stronger publication framing.
  - The Phase 3 local closure decision is now evidence-based: `NB05`–`NB09` all executed successfully again on 2026-05-19, so Model Card + ROI are now backed by fresh local artifacts rather than by stale documentation only.
- On 2026-05-21, the housekeeping alignment requested by the user was applied in both the local workflow and Drive copy:
  - obsolete `reports/*20260502*` outputs were removed where present
  - `reports/churn_monitoring_dashboard_20260512.html`, `reports/model_card_v2_20260512.md`, and `reports/roi_simulation_20260512.html` were removed
  - Python `__pycache__` folders under `src/` and `tests/` were removed
  - `n8n/daily_churn_retention_workflow_20260502.json` was removed locally where present
  - Drive-side misplaced `.csv`, `.parquet`, and `.joblib` artifacts were re-homed from `reports/` into `data/processed/` and `models/`, then verified
- Also on 2026-05-21, the proposed action table was exported as a compact 4-column semicolon-separated CSV for easier review and spreadsheet import:
  - `reports/proposed_action_table_4cols_20260521.csv`
  - columns: `customer_unique_id`, `snapshot_date`, `risk_tier`, `proposed_action`
- Later on 2026-05-21, the user-reviewed workspace↔Drive discrepancy sheet was applied with the agreed clarifications:
  - Drive-canonical files such as `.dockerignore`, `.gitattributes`, `.gitignore`, `Dockerfile`, `RELEASE_NOTES_v1.md`, selected `_private/` files, `requirements.txt`, `assets/images/n8n_workflow_phase2.png`, and `n8n`/workflow support references were replicated into the workspace where requested.
  - Obsolete local-only files from older project states were removed from the workspace, including old `20260502` processed/model artifacts, local-only `_private/_agents` and `_deprecated_logos` files, `notebooks/Workflow.xlsx`, and dated `n8n` workflow snapshots.
  - Drive-only deprecated notebook files under `notebooks/_deprecated/` and lingering spreadsheet exports under `reports/` were removed from Drive where requested.
  - Workspace-canonical files including `notebooks/08_n8n_orchestration.ipynb`, `reports/model_card_v2_20260519.md`, `reports/roi_simulation_20260519.html`, `data/processed/generated_coupons.jsonl`, `data/processed/retention_actions_20260519.parquet`, `scripts/ensure_churn_api_62881.sh`, `src/api/__init__.py`, `src/api/churn_service.py`, and `tests/test_churn_service.py` were copied to Drive.
  - Runtime log files `logs/churn_api_62881.log` and `logs/churn_api_62881.pid` were intentionally kept local-only by explicit agreement and not synchronized to Drive.
  - Verification succeeded for all targeted actions except `_private/Hoja de cálculo sin título.xlsx`, which copied across but still presents a content mismatch between workspace and Drive and should be rechecked before treating it as fully aligned.
- Later on 2026-05-21, the user approved removal in both workspace and Drive of now-nonessential `20260506` processed artifacts that no longer apply to the latest notebook line:
  - `data/processed/churn_diagnostics_20260506.csv`
  - `data/processed/churn_driver_summary_20260506.csv`
- `data/processed/churn_explainability_20260506.parquet`
  - `data/processed/churn_inference_smoke_test_20260506.parquet`
  - `data/processed/retention_actions_20260506.parquet`
  - Removal was verified in both locations.
- On 2026-05-21 evening, the notebook chain was hardened against cross-run drift and non-reproducible orchestration ids:
  - `NB02` now uses the same robust project-root resolution pattern as `NB01`.
  - `NB04` now persists `run_id`, `run_date_tag`, `model_version`, `pipeline_tag`, and training timestamp inside the model package, anchored to the feature artifact tag instead of a fresh wall-clock date.
  - `NB05` and `NB06` now rebuild diagnostics/explainability outputs against the persisted `run_date_tag` from `NB04`, so downstream artifacts stay aligned with the same canonical run.
  - `NB07` now validates and packages only same-run feature / prediction / explainability artifacts, and carries the shared run metadata into the scoring bundle.
  - `NB08` now validates the scoring-bundle run tag before generating orchestration outputs and builds `offer_code_stub` from `customer_unique_id` instead of the dataframe index.
  - `README.md` now clarifies that `STATUS.md` is the operational traceability log, that `20260506` is the canonical shared artifact tag, and that the publication wording consistently uses the `canonical V2C artifact line` / `internal-pilot-first` framing.
- On 2026-05-22 early morning, the user-approved refresh from Drive was applied for `README.md` and `.gitignore` from `Portfolio/daily-customer-churn-predictor` into the canonical local project tree `projects/TFM/daily-customer-churn-predictor`, and both files were byte-verified against Drive after copy.
- On 2026-05-23, the first implementation block of `PHASE3_unified_integration_plan_v3.md` was started in the canonical local repo with the first contract-normalization pass:
  - `src/api/churn_service.py` health output now surfaces `run_id`, `run_date_tag`, `model_version`, `pipeline_tag`, and source metadata from the latest scoring bundle.
  - `src/api/churn_service.py` coupon generation now accepts the canonical Phase 3 keys `customer_unique_id` and `risk_tier` (while remaining backward compatible with legacy aliases) and returns canonical lineage metadata.
  - `tests/test_churn_service.py` was updated and revalidated against the new canonical API contract.
  - `notebooks/08_n8n_orchestration.ipynb` was normalized so HIGH-tier `primary_channels` now match the actual operational scope (`email,push`) and the outdated SMS follow-up wording was removed from the current workflow narrative.
  - `n8n/n8n_workflow_daily_churn_retention_workflow.json` was normalized away from legacy `customer_id` / `risk_level` references and now carries `customer_unique_id`, `risk_tier`, `run_id`, and `run_date_tag` in the coupon-generation and SQL-log paths.
  - `src/pipeline/load_predictions.py` was created as the first repo-aligned Phase B loader baseline with freshness validation, required-column checks, idempotent load behavior, and automatic table/index creation against the configured SQLAlchemy target.
  - The loader was then upgraded from the earlier prediction-only shape to the canonical retention-payload shape expected by the workflow (`snapshot_key`, `recommended_offer_type`, `primary_channels`, `control_group_flag`, `send_action_flag`, `offer_code_stub`, plus normalized lineage fields), including fallback inference for `run_id` / `run_date_tag` when older payload files omit them.
  - `db/migrations/001_add_run_date_dedup.sql` was created as the canonical DDL baseline for `churn_predictions` plus the unique dedup index on `(customer_unique_id, run_date)`.
  - `db/migrations/002_opt_outs_and_governance.sql` was added as the initial suppression/governance baseline with `opt_outs`, `retention_actions_skipped`, and `retention_governance_config` tables plus default send-window and daily-cap values.
  - Repo-level runtime artifacts `docker-compose.yml` and `.env.example` were added to anchor the hardened deployment shape around Postgres + the scoring API service on port `62881`.
  - Real execution validation confirmed the loader can populate a fresh SQLite target from the canonical `retention_actions_20260506.parquet` artifact with `3346` rows and the unique dedup index present; a second run remains idempotent.
- On 2026-05-24, the next Phase 3 execution block was applied locally on the canonical repo:
  - `src/pipeline/load_predictions.py` was adjusted again so the insert idempotency now targets the live primary key constraint on `(customer_unique_id, snapshot_key)`, avoiding `UniqueViolation` against historical rows already present in Postgres while preserving no-op behavior on replays.
  - `src/pipeline/load_predictions.py` now validates parquet columns against the parquet contract itself (using `snapshot_date` rather than `scored_date`) and maps `snapshot_date -> scored_date` only during the Postgres insert-preparation step.
  - `src/pipeline/load_predictions.py` was realigned to the live Postgres `churn_predictions` schema: the insert path now maps only the approved operational columns, derives `run_date` from `snapshot_key` (`YYYYMMDD`), generates `loaded_at` at insert time, removes non-schema fields such as `snapshot_date`, `model_version`, `pipeline_tag`, `run_id`, and `run_date_tag`, and validates the target table instead of attempting to add columns to it.
  - `src/pipeline/load_predictions.py` was updated so `_ensure_table()` now inspects `churn_predictions` through Postgres-compatible `information_schema.columns` instead of SQLite `PRAGMA table_info`, preserving the additive column backfill logic while keeping the loader aligned with the intended Postgres runtime.
  - `src/api/churn_service.py` now resolves `PROJECT_ROOT` from the `PROJECT_ROOT` environment variable when provided, with the previous `Path(__file__).resolve().parents[2]` logic retained as fallback for repo-local execution.
  - The migration files requested from `PHASE3_unified_integration_plan_v3.md` sections `B-1`, `B-2`, and `B-3` were aligned in the repo: `db/migrations/001_add_run_date_dedup.sql` now carries the documented `ALTER TABLE` + dedup index baseline, `db/migrations/002_create_opt_outs.sql` was added with the documented suppression-table definition, and `db/migrations/003_retention_action_logs.sql` now also includes the missing retention-events dedup index.
  - `src/pipeline/load_predictions.py` was hardened again so the canonical retention payload validates score bounds and valid `risk_tier` values, ensures the suppression table exists before load, and still backfills `run_id` / `run_date_tag` from the parquet tag when older payload artifacts do not yet carry those fields.
  - `n8n/n8n_workflow_daily_churn_retention_workflow.json` was upgraded from a documentation-only baseline into an executable Phase 3 draft with a loader node, scoring-API health check, suppression + dedup preselection query, a governance code node enforcing send-window / tier-cap filtering before coupon generation, and a skip-log query aligned with the current `retention_actions_skipped` schema.
  - `tests/test_load_predictions.py` was added to validate loader insertion, `opt_outs` bootstrap behavior, and fallback inference of missing run metadata on a fresh SQLite target.
  - `scripts/reconcile_local_phase3_sqlite.py` was added and executed successfully to rebuild the local SQLite `churn_predictions` table onto the richer Phase 3 operational shape while preserving a `churn_predictions_backup_pre_phase3` backup table.
  - `db/migrations/003_retention_action_logs.sql` was added to establish canonical `retention_actions` / `retention_events` operational tables and indexes for the hardened workflow contract.
  - `src/pipeline/detect_conversions.py` plus `n8n/n8n_workflow_conversion_detection.json` were added as the first executable Phase C baseline for conversion detection over the 14-day window.
  - `.env.example` now documents `SOURCE_DB_URL` and `CONVERSION_WINDOW_DAYS`, and `tests/test_detect_conversions.py` now validates the conversion-detection insert path on a fresh SQLite setup.
  - Real execution validation succeeded for `python3 -m py_compile src/pipeline/load_predictions.py src/api/churn_service.py scripts/reconcile_local_phase3_sqlite.py src/pipeline/detect_conversions.py`, `python3 -m unittest tests.test_churn_service tests.test_load_predictions tests.test_detect_conversions -v`, and the local SQLite reconciliation run itself.

## Current findings
- The v1 model remained extremely high-recall/high-positive under the original 90-day churn definition, which kept average precision near 0.994 but yielded a more modest ROC AUC around 0.589 on the test split.
- The canonical `V2C` base is structurally valid with `9,571` rows, `1,795` unique customers, `14` snapshots, and prevalence around `0.9748`.
- The selected adaptive horizon is now tighter than the first v2 attempt, with bounds `75-150` days and average horizon around `93.64` days.
- `NB04` selected **XGBoost** as the best model again on the canonical `V2C` line.
- On the canonical v2 test split, the selected model reached approximately:
  - `ROC AUC = 0.8016`
  - `Average Precision = 0.9937`
  - `Precision@Top 5% = 1.0000`
  - `Precision@Top 10% = 0.9970`
- `NB05` diagnostics confirm that the ranking remains very strong in aggregate, but also reinforce the same caveat seen earlier: the target is still overwhelmingly positive, so calibration and threshold interpretation must be handled cautiously.
- The quantile-threshold diagnostic remains operationally useful even under that caveat: top-ranked slices preserve very high observed churn rates, which supports the retention prioritization logic.
- `NB06` explainability shows that the current score is driven predominantly by frequency, monetary, and recency patterns in the scored sample, and the explainability layer yields action-ready segments through driver grouping, LTV quartiles, recommended offer types, discount guidance, and VIP human-touch flags.
- `NB07` now packages the canonical V2C inference logic with percentile-based risk tiers and attached retention rules instead of the legacy fixed-threshold policy.
- `NB08` translates the scored population into a concrete retention payload aligned with the confirmed retention-strategy document, including journey sequencing, control-group handling, driver-sensitive offer mapping, and channel policy.
- `NB09` consolidates diagnostics, explainability, and campaign-readiness outputs into a single monitoring dashboard for stakeholder review.
- The publication layer now reflects the current local candidate coherently instead of leaving the repository framed as a v1-only baseline.
- The main unresolved analytical caveat is unchanged: even the winning `V2C` formulation still has a very high positive prevalence, so downstream diagnostics should continue to interpret the target cautiously.
- On 2026-05-31, `NB05` was hardened and re-executed against the canonical scoring bundle path instead of the now-removed legacy `churn_model_*.joblib` artifact, so the diagnostics notebook is again executable after the cleanup pass.
- The refreshed calibration layer now compares raw, sigmoid, and isotonic probability mappings on the canonical V2C line:
  - raw: `ROC AUC 0.8016`, `AP 0.9937`, `Brier 0.0572`, mean calibration gap `0.1066`
  - sigmoid: `ROC AUC 0.8016`, `AP 0.9937`, `Brier 0.0224`, mean calibration gap `0.0109`
  - isotonic: `ROC AUC 0.7919`, `AP 0.9920`, `Brier 0.0229`, mean calibration gap `0.0082`
- The evidence therefore supports a more senior-quality reading of the canonical V2C line: raw scores preserve ranking strength, but calibrated variants materially improve probability reliability, with sigmoid keeping ranking unchanged and isotonic minimizing average calibration gap.
- New calibration evidence artifacts produced locally:
  - `data/processed/churn_calibration_comparison_20260519.csv`
  - refreshed `data/processed/churn_diagnostics_20260519.csv`
  - refreshed `reports/model_diagnostics_20260519.html`
  - refreshed executed notebook `notebooks/05_model_evaluation_diagnostics.ipynb`

## Reproducibilidad
- Estado: VERIFICADA
- Evidencia principal: `reports/reproducibility/CP5_EXTERNAL_VALIDATION_EVIDENCE.txt`
- Resultado: validación externa completada con `docker compose up --build` exitoso, `api + web` healthy, smoke/API checks correctos y `docker compose down` limpio
- Observación menor: Docker emitió una advertencia no bloqueante sobre `version` obsoleto en `cp5_external_validation_package/docker-compose.yml`
- Bloqueador: No
- Update 2026-08-11: the earlier Python-version-sensitive f-string issue had already been corrected before this successful rerun, which allowed the external Docker validation to pass cleanly.

## Next pending step
- If Alberto wants to continue after this closure, the next step is review/approval of the Phase 7.1 reproducibility hardening outputs already applied in V12 and code/Drive, not any autonomous advance into Fase 8/9/10/11/12.
- CP5 is now closed with external evidence captured. The next pending gate is CP6 only when Alberto explicitly authorizes the cutover/release decision.
1. Review the verified CP5 evidence in `reports/reproducibility/CP5_EXTERNAL_VALIDATION_EVIDENCE.txt` if needed for sign-off.
2. Keep the legacy production path unchanged until the explicit CP6 cutover authorization.
3. Start CP6 only after Alberto confirms the go-ahead.

## Sync log
- On 2026-08-11 afternoon, the successful CP5 external-validation closure was synchronized to the mounted Drive project copy at `/data/gdrive/Portfolio/daily-customer-churn-predictor`. Verified there: `reports/reproducibility/CP5_EXTERNAL_VALIDATION_EVIDENCE.txt`, updated `reports/reproducibility/CP5_EXTERNAL_VALIDATION_STATUS.md`, updated `STATUS.md`, and updated `_private/_docs/tech_doc.md`.
- On 2026-08-11 late morning, the current external-validation state was synchronized to the mounted Drive project copy at `/data/gdrive/Portfolio/daily-customer-churn-predictor` (runtime-equivalent of the requested mounted Drive target). Verified there: updated project root files, refreshed `reports/`, refreshed code/docs directories, `reports/reproducibility/CP5_EXTERNAL_VALIDATION_STATUS.md`, and populated `cp5_external_validation_package/`. Sensitive/banned items were also re-checked out of the Drive copy: no root `.env`, no `cp5_external_validation_package/.env`, no `__pycache__/`, and no `tmp_*` directories in the verified paths.
- On 2026-08-01 late evening, the final interview-copy layer was synchronized to the mounted Drive copy and verified successfully: `README.md`, `STATUS.md`, `_private/_docs/tech_doc.md`, and `reports/vivamarket_portfolio_interview_copy_20260801.md`.
- On 2026-08-01 late evening, the README/deck tone-polish follow-up and public-access incident notes were synchronized to the mounted Drive copy and verified successfully: `README.md`, `STATUS.md`, `_private/_docs/tech_doc.md`, `reports/vivamarket_portfolio_business_deck_20260801.html`, and `reports/vivamarket_portfolio_business_deck_20260801.pdf`.
- On 2026-08-01 evening, the business-portfolio repackaging artifacts were synchronized to the mounted Drive copy and verified successfully: `README.md`, `STATUS.md`, `_private/_docs/tech_doc.md`, `assets/images/portfolio/phase7_reporting_page_live_20260801.png`, `assets/images/portfolio/phase7_review_page_live_20260801.png`, `reports/vivamarket_portfolio_executive_summary_20260801.html`, `reports/vivamarket_portfolio_executive_summary_20260801.pdf`, `reports/vivamarket_portfolio_business_deck_20260801.html`, and `reports/vivamarket_portfolio_business_deck_20260801.pdf`.
- On 2026-08-01, the Phase 7 review-page cold-start fix was synchronized to the mounted Drive copy and verified successfully: `src/evidence/phase7_integration.py`, `src/api/phase7_review_page.py`, `tests/test_phase7_integration.py`, `tests/test_phase7_review_page.py`, `_private/_docs/tech_doc.md`, and `STATUS.md`.
- On 2026-07-31 late morning, `STATUS.md` was corrected to realign the `Next pending step` section with `_private/phase7_action_plan_v41.md`: the outdated implementation-open items were removed, and the remaining Phase 7 work is now recorded as independent verification + non-blocking maintenance only.
- On 2026-07-28 early afternoon, the second local `7.1.6` governed-launch-request slice was synchronized to the mounted Drive copy and verified successfully: `src/evidence/phase7_integration.py`, `src/api/churn_service.py`, `tests/test_phase7_integration.py`, `tests/test_churn_service.py`, `data/processed/phase7_integrated_actions_20260727.json`, `data/processed/phase7_action_history_log.parquet`, `data/processed/phase7_stat_launch_requests.parquet`, `reports/phase7_integrated_actions_20260727.md`, `_private/_docs/tech_doc.md`, and `STATUS.md`.
- On 2026-07-28 midday, the first local `7.1.6` lifecycle/governance slice was synchronized to the mounted Drive copy and verified successfully: `src/evidence/phase7_integration.py`, `src/api/churn_service.py`, `tests/test_phase7_integration.py`, `tests/test_churn_service.py`, `data/processed/phase7_integrated_actions_20260727.json`, `reports/phase7_integrated_actions_20260727.md`, `_private/_docs/tech_doc.md`, and `STATUS.md`.
- On 2026-07-28 morning, the fourth local `7.1.5` slice was synchronized to the mounted Drive copy and verified successfully: `src/evidence/phase7_integration.py`, `src/api/churn_service.py`, `scripts/build_phase7_kpi_status.py`, `scripts/build_phase7_n8n_payload.py`, `tests/test_phase7_integration.py`, `tests/test_churn_service.py`, `data/processed/phase7_kpi_status_20260727.json`, `reports/phase7_kpi_status_20260727.md`, `data/processed/phase7_n8n_payload_20260727.json`, `_private/_docs/tech_doc.md`, and `STATUS.md`.
- On 2026-07-28 morning, the third local `7.1.5` slice was synchronized to the mounted Drive copy and verified successfully: `src/evidence/phase7_integration.py`, `src/api/churn_service.py`, `tests/test_phase7_integration.py`, `tests/test_churn_service.py`, `_private/_docs/tech_doc.md`, and `STATUS.md`.
- On 2026-07-28 morning, the second local `7.1.5` slice was synchronized to the mounted Drive copy and verified successfully: `scripts/launch_phase7_stat_test.py`, `tests/test_phase7_integration.py`, `data/processed/phase7_integrated_actions_20260727.json`, `data/processed/phase7_stat_test_runs.parquet`, `_private/_docs/tech_doc.md`, and `STATUS.md`.
- On 2026-07-28 morning, the first local `7.1.5` integration slice was synchronized to the mounted Drive copy and verified successfully: `src/evidence/phase7_integration.py`, `scripts/build_phase7_integrated_actions.py`, `tests/test_phase7_integration.py`, `data/processed/phase7_integrated_actions_20260727.json`, `reports/phase7_integrated_actions_20260727.md`, `_private/_docs/tech_doc.md`, and `STATUS.md`.
- On 2026-07-28 early morning, the Phase 7 `7.1.3-bis` default-model implementation and first real full-pipeline verification artifacts were synchronized to the mounted Drive copy and verified successfully: `src/evidence/phase7_proposal_builder.py`, `data/processed/phase7_action_drafts_20260727.json`, `reports/phase7_action_drafts_20260727.md`, and `STATUS.md`.
- On 2026-07-27 evening, the external draft package for `MODE=synthetic_demo` was synchronized to the mounted Drive copy and verified successfully: `data/synthetic_demo/phase7_source_snapshot_20260727.json`, `data/synthetic_demo/phase7_action_drafts_20260727.json`, `data/synthetic_demo/phase6_kpi_status_20260727.json`, `data/synthetic_demo/README_synthetic_demo.md`, `_private/_docs/tech_doc.md`, and `STATUS.md`.
- On 2026-07-22 just after midnight, the second Phase 6 implementation cut was synchronized to the mounted Drive copy and verified successfully: `reports/phase6_dynamic_evidence_recommendations_20260721.md`, `reports/phase6_validation_checklist_20260721.md`, `scripts/build_phase6_recommendations.py`, `src/evidence/recommendation_builder.py`, `tests/test_evidence_sourcing.py`, `_private/_docs/tech_doc.md`, and `STATUS.md`.
- On 2026-07-21 evening, the first executable Phase 6 implementation cut was synchronized to the mounted Drive copy and verified successfully: `docs/phase6_spec_dynamic_evidence_system.md`, `config/evidence_sources_allowlist.yaml`, `data/interim/phase6_seed_evidence.json`, `data/processed/evidence_catalog_20260721.json`, `reports/phase6_evidence_catalog_summary_20260721.md`, `scripts/build_phase6_catalog.py`, `src/evidence/catalog_builder.py`, `tests/test_phase6_catalog_builder.py`, `_private/_docs/tech_doc.md`, and `STATUS.md`.
- On 2026-07-21, the pre-implementation Phase 6 documentation correction was synchronized to the mounted Drive copy and verified successfully: `_private/phase6_action_plan_v3.md`, `_private/_docs/tech_doc.md`, and `STATUS.md`.
- On 2026-06-08 overnight, the new parallel Phase 5 comparison-preparation artifacts were synchronized to the mounted Drive copy and verified successfully: `notebooks/10_phase5_autonomous_agent_ab_analysis.ipynb`, `_private/human_hours_log.csv`, `STATUS.md`, and `_private/tech_doc.md`.
- On 2026-06-08 early morning, the refreshed Block G workstream files were synchronized again to the mounted Drive copy and byte-verified successfully after the new reconciliation pass and monitor clarification update: `src/pipeline/phase5_shadow_monitor.py`, `tests/test_phase5_shadow_monitor.py`, `reports/phase5_shadow_monitor_latest.html`, `reports/phase5_shadow_divergences.md`, `STATUS.md`, and `_private/tech_doc.md`.
- On 2026-06-07, the new Block G shadow-monitoring workstream files were synchronized to the mounted Drive copy and byte-verified successfully: `src/pipeline/phase5_shadow_monitor.py`, `tests/test_phase5_shadow_monitor.py`, `reports/phase5_shadow_monitor_latest.html`, `reports/phase5_shadow_divergences.md`, `STATUS.md`, and `_private/tech_doc.md`.
- On 2026-05-26, the user-provided export `n8n_workflow_daily_churn_retention_workflow.json` was installed into the canonical local project under `n8n/`, then synchronized to the mounted Drive copy and byte-verified successfully.
- Later on 2026-05-26, TD-15 and TD-16 were applied to that canonical workflow file: `LIMIT 10` was removed from `Read Predictions`, and a new LOW-risk branch was added to `Risk Switch` so low-tier records are written to `retention_actions_skipped` with reason `low_tier_dispatch_deferred_td12` and no active dispatch. The updated workflow file was then synchronized again to Drive and byte-verified successfully.
- Later on 2026-05-26, the pre-publication closure pass for TD-07 / TD-08 / README alignment was also completed: `docker-compose.yml` was sanitized to stop exposing inline Postgres credentials, `.env.example` was corrected from `churn_ops` to `vivamarket` and expanded with env-driven Postgres variables, and the public README workflow narrative was updated to reflect the post-TD-15 / TD-16 state (all eligible records, explicit LOW branch, LOW skip logging). These three files were then synchronized to Drive and byte-verified successfully.
- Later on 2026-05-26, the final pre-release pass for checklist points 5 / 6 / 7 was completed: reproducibility sanity checks passed (`tests.test_churn_scoring`, `tests.test_churn_service`, and `py_compile` for the core scoring/API/pipeline modules), the README public status narrative was updated to frame the baseline as a Phase 3B operational closure, and `RELEASE_NOTES_v3.0.0-phase3b.md` was created as the prepared release-notes artifact. The updated README and new release-notes file were synchronized to Drive and byte-verified successfully.
- Later on 2026-05-26, the support-only files `PHASE3_unified_integration_plan_v3.md`, `reports/workspace_drive_diff_proposal_20260521.csv`, and `reports/workspace_drive_diff_proposal_20260521.gsheet` were removed from both the canonical workspace and Drive copy by user request because they do not add public project value under the current publication criterion.
- Later on 2026-05-26, release-history tracking was consolidated into a single internal `RELEASE_NOTES.md` file, the older split files `RELEASE_NOTES_v1.md` and `RELEASE_NOTES_v3.0.0-phase3b.md` were removed from workspace and Drive, and `.gitignore` was updated so both `STATUS.md` and `RELEASE_NOTES.md` remain internal-history files outside the public repo scope.
- Later on 2026-05-26, the public README was rewritten again to align the narrative with the verified Phase 3 state plus the Phase 4 action plan: The Architect wording now uses `personal Spec-Driven Data Science agent/workflow`, `Future Work` now centers explicitly on Phase 4 as the next implementation stage, the version note was tightened around Phase 3B operational closure, and the footer date/status were refreshed. The updated README was synchronized to Drive and byte-verified successfully.
- A final same-day README consistency pass then removed the stale `rclone` operational note, replaced remaining Phase-3-future wording with Phase-3B/Phase-4 framing, clarified OpenClaw as the execution environment/agent runtime, and aligned the layer description plus n8n milestone wording with the 2026-05-18 validation and 2026-05-26 hardening sequence. The refreshed README was synchronized again to Drive and byte-verified successfully.
- Also on 2026-05-26, the support/planning documents `PHASE4_action_plan_v3.md` and `AUTOMATION_SYSTEM_PLAN.md` were removed from the local public project tree by user request after confirming the user already keeps them in a private Drive location.
- Also on 2026-05-26, the older `20260506` reports and processed artifacts that now have `20260519` replacements were removed from both workspace and Drive by user request, preserving the newer artifact line as the kept public/project-facing state.
- Also on 2026-05-26, a revised private planning document `_private/PHASE4_action_plan_v5_1.md` was created from the user-updated Phase 4 action plan, incorporating execution-grade adjustments for holdout timing, holdout stability, closed-evaluation definition, action-traceability fields, and workflow idempotence. The file was synchronized to Drive and verified by direct read in both locations.
- Also on 2026-05-26, TD-14 was investigated and clarified: `data/processed/churn_explainability_20260519.parquet` is still present in the canonical local project tree and remains the active explainability source consumed by `src/api/churn_service.py` through dynamic latest-file resolution. The current n8n workflow reaches explainability through the API endpoint rather than a hardcoded parquet filename, so TD-14 closes as a lineage/documentation issue instead of a regeneration task.
- Also on 2026-05-26, Block B traceability hardening was started and a first executable baseline was applied locally: `db/migrations/004_phase4_traceability_hardening.sql` adds `holdout`, `holdout_reason`, and `campaign_cycle` to `retention_actions` plus an idempotence-oriented unique index on `(customer_unique_id, run_date_tag, action_type, channel, holdout)`. The active n8n workflow JSON was also realigned so `Generate Coupon` uses canonical request keys, `Log Skipped` writes canonical skip-lineage fields, and `Log Actions` now logs canonical traceability fields (`customer_unique_id`, `risk_tier`, `run_id`, `run_date_tag`, channel, metadata, campaign cycle) with `ON CONFLICT DO NOTHING` protection against duplicate same-run action inserts. Regression validation passed via `tests.test_churn_service`, `tests.test_load_predictions`, `tests.test_detect_conversions`, and `py_compile`.
- Also on 2026-05-26, Block B-3 received a first operational holdout baseline: `notebooks/08_n8n_orchestration.ipynb` now assigns holdout/control rows deterministically from `customer_unique_id` instead of using the older random HIGH-only sample, producing an approximately 15% stable holdout split across the scored population. The active n8n workflow JSON was updated so `Read Predictions` includes both dispatchable and holdout rows, a new `Holdout Gate` routes control-group rows away from coupon generation, and a new `Log Holdout` node writes explicit `holdout_no_dispatch` records into `retention_actions` with `holdout = TRUE` and reason metadata. A quick parity check on the current retention parquet showed 502 / 3346 rows in holdout (~15.0%), confirming the baseline behaves as intended.
- Also on 2026-05-26, the holdout policy itself was tightened beyond the first split: `db/migrations/005_phase4_holdout_policy.sql` adds `holdout_assignment_key`, `holdout_window_days`, and `holdout_window_end` to `retention_actions`; `notebooks/08_n8n_orchestration.ipynb` now emits those fields for holdout rows using a documented 30-day window; and `Log Holdout` in the active n8n workflow now persists those window/policy fields together with the no-dispatch holdout record. This makes the holdout design explicit enough to support later attribution-window and reassignment rules instead of leaving it as an implicit control flag only.
- Also on 2026-05-26, Block B-4 preparation was hardened without claiming live completion: `Send Push` was corrected so OneSignal now targets `customer_unique_id` instead of the older `customer_id` alias, the workflow notes were tightened around the treated-vs-holdout dispatch gate, and a new private checklist `_private/B4_controlled_dispatch_checklist.md` was added to define the exact validation evidence required for the first real HIGH/MEDIUM controlled cycle. Regression validation still passed after these changes (`tests.test_churn_service`, `tests.test_load_predictions`, `tests.test_detect_conversions`).
- Also on 2026-05-26, NB08 (`notebooks/08_n8n_orchestration.ipynb`) was re-executed to regenerate `data/processed/retention_actions_20260519.parquet` under the current holdout policy. During that work, a real artifact-selection defect surfaced: the local prediction artifact was tagged `20260506`, the explainability artifact was tagged `20260519`, and the newer scoring bundle lacked persisted `run_date_tag` metadata. Compatibility checks showed the artifacts were content-aligned (3346/3346 matching customer/snapshot pairs with equal probabilities and risk tiers), so NB08 was hardened to fall back to compatible prediction/explainability pairs when bundle metadata is incomplete. After re-execution, the regenerated retention payload now carries the expected explicit holdout fields and a deterministic 15% holdout shape (`502` holdout / `2844` treated, no holdout-treated overlap). The remaining blocker before calling B-4 complete is still real OneSignal credential validation in the live path.
- On 2026-05-28, that remaining B-4 blocker was resolved in execution evidence: `Send Push` completed end-to-end through n8n, OneSignal authenticated successfully with `HTTP 200` using Header Auth credentials, and the returned `All included players are not subscribed` message was accepted as expected behavior for the current synthetic dataset with no registered devices. `Send Email` remains intentionally disabled because it is a secondary channel not required for B4 closure, LOW activation remains deferred until Block C holdout activation, and Block B exit-gate items are now all completed.
- Also on 2026-05-28, the user-provided corrected main workflow export was installed as the canonical `n8n/n8n_workflow_daily_churn_retention_workflow.json`, the README image asset `assets/images/n8n_workflow_phase2.png` was replaced with the refreshed screenshot, and obsolete dated duplicate workflow files were removed from both workspace and Drive. After a final repo-vs-instance review with the user, the non-deployed helper file `n8n/n8n_workflow_conversion_detection.json` was also removed from both locations, leaving only the active canonical n8n JSON set (`n8n_workflow_daily_churn_retention_workflow.json` and `n8n_workflow_error_handler_workflow.json`). The refreshed main workflow JSON, screenshot, and folder cleanup were synchronized to Drive and verified successfully.
- Also on 2026-05-28, the first concrete Phase 4 implementation step beyond documentation was started in code: `src/api/churn_service.py` now exposes `POST /events/onesignal` for OneSignal event ingestion and `GET /health/events` for ingestion health/status, with idempotent inserts into `retention_events` keyed by provider-event identity for duplicate-safe delivery handling. Test coverage was extended in `tests/test_churn_service.py` for health, ingestion, and idempotence, and validation passed with `python3 -m unittest tests.test_churn_service -v` plus `python3 -m py_compile src/api/churn_service.py`.
- Also on 2026-05-28, the conversion-attribution baseline was upgraded to match the Phase 4 causal-measurement design more closely: `src/pipeline/detect_conversions.py` no longer uses a single flat attribution window and now applies tier-specific windows (`HIGH=14`, `MEDIUM=21`, `LOW=30`) while carrying `risk_tier`, `holdout`, and `conversion_window_days` into the event metadata written to `retention_events`. Test coverage in `tests/test_detect_conversions.py` was expanded to validate the HIGH baseline plus MEDIUM/LOW tier-window behavior, and regression validation passed with `python3 -m unittest tests.test_detect_conversions tests.test_churn_service -v`.
- On 2026-05-30, the next Phase 4 measurement step was implemented locally: new utility module `src/pipeline/campaign_kpis.py` now computes the explicit **closed conversion evaluation** unit over `retention_actions` + `retention_events`, counts both treated and holdout rows once their tier-specific attribution windows have elapsed, flags conversion status within-window, and produces pre-holdout KPI summaries including per-tier conversion rates and holdout lift where both groups are available. New regression coverage was added in `tests/test_campaign_kpis.py`, and validation passed with `python3 -m unittest tests.test_campaign_kpis tests.test_detect_conversions tests.test_churn_service -v`.
- Later on 2026-05-30, the KPI/evaluation module was aligned with the new synthetic Block D canonical data contract delivered by the user in `data/processed/`: `src/pipeline/campaign_kpis.py` now supports both the earlier database-oriented local schema and the target Phase 4 parquet schema (`action_id`, `tier`, `action_ts`, `attr_window_days`, `campaign_cycle`, event linkage by `action_id`). Real validation was then executed against the copied synthetic parquets (`retention_actions_synthetic_30d.parquet` and `retention_events_synthetic_30d.parquet`), and the resulting pre-holdout KPI summary produced coherent closed-evaluation totals plus holdout lift by tier. Regression validation passed with `python3 -m unittest tests.test_campaign_kpis -v`, `python3 -m py_compile src/pipeline/campaign_kpis.py`, and direct script execution of `python3 src/pipeline/campaign_kpis.py`.
- On 2026-05-31, the first stakeholder-facing Phase 4 KPI monitor surface was added on top of that measurement baseline: `src/pipeline/campaign_kpis.py` now renders and writes `reports/phase4_campaign_kpi_monitor_latest.html`, exposing headline KPI cards, a tier/cohort conversion summary, early holdout-lift visibility, and explicit pre-holdout honesty labeling. `tests/test_campaign_kpis.py` was extended to cover the HTML render path, and validation passed with `python3 -m unittest tests.test_campaign_kpis -v` plus direct execution of `python3 src/pipeline/campaign_kpis.py`, which regenerated the latest HTML report successfully.
- Still on 2026-05-31, Block E demo mode was advanced with a dedicated stakeholder BI surface: new module `src/pipeline/phase4_bi_dashboard.py` now builds `reports/phase4_bi_dashboard_demo.html`, explicitly separating the **Operational view** (tier mix, dispatch governance summary, simulated campaign-delivery KPIs) from the **Analytical view** (model-quality snapshot, V1 vs V2C comparison, driver-family summary, and closed-evaluation holdout view). The report follows the Phase 4 honesty rule by labeling campaign-performance panels as `[DEMO — simulated campaign data]` while keeping analytical lineage tied to real canonical artifacts. New regression coverage was added in `tests/test_phase4_bi_dashboard.py`, and validation passed with `python3 -m unittest tests.test_phase4_bi_dashboard tests.test_campaign_kpis -v` plus direct execution of `python3 src/pipeline/phase4_bi_dashboard.py`.
- Later the same morning, the Block E demo dashboard was upgraded from a table-only first version into a stronger analytics surface with embedded Plotly visuals for tier mix, treated-vs-holdout conversion comparison, and top driver-family concentration. The render tests were extended to assert the chart containers / Plotly payload are present, and the dashboard was regenerated successfully again with `python3 -m unittest tests.test_phase4_bi_dashboard tests.test_campaign_kpis -v` plus `python3 src/pipeline/phase4_bi_dashboard.py`.
- Later on 2026-05-31, the core local Block D governance layer was implemented and validated for the portfolio baseline:
  - new module `src/pipeline/phase4_governance_monitor.py` now computes the planned three governance layers over the current portfolio baseline (`feature drift`, `score drift`, and `tier stability`) and evaluates the documented retraining-trigger thresholds in one consolidated pass
  - new report `reports/phase4_governance_monitor_latest.html` was generated as the stakeholder-readable governance/drift surface
  - new machine-readable artifact `data/processed/phase4_governance_monitor_latest.json` was generated for downstream continuity / auditability
  - new `reports/model_card_v3_phase4_demo_20260531.md` was generated to upgrade the governance narrative from the older v2 card to an explicit Phase 4 demo-governance card with synthetic holdout-lift evidence and trigger status
  - regression coverage was added in `tests/test_phase4_governance_monitor.py`
  - validation passed with `python3 -m unittest tests.test_phase4_governance_monitor tests.test_campaign_kpis tests.test_phase4_bi_dashboard -v`, `python3 -m py_compile src/pipeline/phase4_governance_monitor.py`, and direct execution of `python3 src/pipeline/phase4_governance_monitor.py`
- Under the current portfolio/demo interpretation, Block D is now treated as closed at the local governance baseline level: the drift layers, trigger rules, and Model Card v3-equivalent governance surface are all present and evidenced locally, while Block E demo is already closed.
- Later on 2026-05-31, Block F was executed as a documented population-redesign benchmark over the user-approved historical synthetic campaign baseline:
  - new module `src/pipeline/phase4_population_redesign_benchmark.py` now benchmarks `retainable` vs `structural_single_purchase` segments from the closed synthetic evaluation base, using per-customer closed-evaluation history, treated/holdout conversion behavior, and dominant-tier/action profile summaries
  - new artifacts were generated:
    - `data/processed/phase4_population_redesign_benchmark_20260531.json`
    - `reports/phase4_population_redesign_benchmark_20260531.html`
    - `reports/phase4_population_redesign_decision_20260531.md`
  - regression coverage was added in `tests/test_phase4_population_redesign_benchmark.py`
  - validation passed with `python3 -m unittest tests.test_phase4_population_redesign_benchmark tests.test_phase4_governance_monitor tests.test_campaign_kpis tests.test_phase4_bi_dashboard -v`, `python3 -m py_compile src/pipeline/phase4_population_redesign_benchmark.py`, and direct execution of `python3 src/pipeline/phase4_population_redesign_benchmark.py`
- The resulting Block F decision currently supports the redesign hypothesis at benchmark level rather than forcing an immediate replacement of V2C: the synthetic benchmark identified a retainable candidate segment representing ~34.8% of customers (`1163 / 3346`) with stronger aggregate treated-vs-holdout lift (`~2.81 pp`) than the structural segment (`~0.94 pp`). This is sufficient to close Block F as a documented portfolio benchmark/decision layer while still leaving any future `v4.0.0` retraining implementation as a separately approved execution step.
- Under the current portfolio/demo interpretation, Phase 4 can now be treated as closed at the documented benchmark baseline: Block B closed, Block C closed, Block D closed, Block E demo closed, and Block F closed as a benchmarked decision artifact set.
- On 2026-05-31, the previously agreed cleanup discrepancy was enforced again across workspace/Drive boundaries: `reports/workspace_drive_diff_proposal_20260521.xlsx` was removed from Drive where it still remained, local `.gitattributes` was removed by explicit user request, and `db/migrations/002_opt_outs_and_governance.sql` was copied to Drive and byte-verified so the migration is now reviewable there as requested.
- Later the same day, the user-approved final cleanup pass was applied in both workspace and Drive: `.venv/`, `logs/`, `models/churn_model_20260506.joblib`, `models/churn_scoring_package_20260506.joblib`, and `db/migrations/002_create_opt_outs.sql` were removed; `.gitignore` was reinforced to keep `.env`, `_private/`, all `__pycache__/`, `logs/`, `.pid`, and the public-but-heavy `data/raw/*.sqlite` dataset out of versioned scope; and `README.md` was updated to state that the raw SQLite source is public and downloadable from Kaggle rather than stored as a normal repo artifact.
- Also on 2026-05-31, after reviewing an external improvement proposal, the user approved a corrected internal execution plan focused on **portfolio senior uplift without scope drift**. That plan was recorded in `_private/PHASE4B_portfolio_senior_uplift_plan_v1.md` and narrows the next formal work to four approved lines only: threshold synchronization hardening, calibration evaluation, Phase 4 HTML visual alignment, and final narrative/documentation tightening. A real holdout/dispatch activation remains explicitly deferred as future scope rather than current work.
- Later the same day, the first uplift implementation block was started on the canonical workflow contract: `n8n/n8n_workflow_daily_churn_retention_workflow.json` was hardened so `Risk Switch` now routes on canonical `risk_tier` emitted by the scoring lineage instead of recomputing HIGH/MEDIUM with hardcoded `0.75/0.45` thresholds. In parallel, `src/api/churn_service.py` was extended to expose the active `risk_thresholds` in `/health` and a dedicated `/thresholds/latest` endpoint, and `tests/test_churn_service.py` was expanded accordingly. This keeps the demo baseline honest while removing the most obvious silent scoring-vs-orchestration drift path.
- Also on 2026-05-31, the second uplift block was implemented using the analytics-reporting UX approach: `src/pipeline/phase4_governance_monitor.py` and `src/pipeline/phase4_population_redesign_benchmark.py` were restyled so the generated HTML reports now follow a light branded presentation with logo header, stronger section hierarchy, explicit executive-summary/decision blocks, semantic HIGH/MEDIUM/LOW tier styling, visible demo honesty labeling, and run/baseline context. The refreshed reports were regenerated and validated through the phase-specific unit tests plus direct script execution.
- Later the same day, the third approved uplift block was executed inside `NB05` (`notebooks/05_model_evaluation_diagnostics.ipynb`) as a real calibration-evaluation closure on the canonical V2C line. The notebook was updated to consume the current `churn_scoring_package_*.joblib` artifact instead of the removed legacy `churn_model_*.joblib` path, then re-executed successfully. The refreshed diagnostics now compare raw vs sigmoid vs isotonic calibration explicitly and export `data/processed/churn_calibration_comparison_20260519.csv` alongside refreshed `churn_diagnostics_20260519.csv` and `reports/model_diagnostics_20260519.html`. Result summary: raw keeps the strongest original ranking profile but is poorly calibrated (`Brier 0.0572`, mean gap `0.1066`), while sigmoid preserves ranking and materially improves probability reliability (`Brier 0.0224`, mean gap `0.0109`); isotonic achieves the lowest mean gap (`0.0082`) with a small ranking tradeoff (`ROC AUC 0.7919`). This closes the calibration-evaluation block and leaves only the final narrative/documentation tightening block pending in the approved uplift plan.
- Also on 2026-05-31, the fourth and final approved uplift block was applied through narrative/documentation tightening. `README.md` was updated so the public closure summary now includes the calibration-evidence decision explicitly, the interpretation section now distinguishes raw vs sigmoid vs isotonic behavior more honestly, the orchestration narrative now reflects the canonical emitted `risk_tier` contract instead of stale hardcoded threshold routing, and the limitations/future framing now separates synthetic demo measurement from true live telemetry more clearly. With that pass, the formal Phase 4B portfolio senior uplift plan is now fully closed at the local baseline level.
- Later the same day, Alberto chose option `1` from the proposed next-step menu (`release/publication pass`). Work then shifted from implementation to packaging the already-closed baseline more cleanly for publication. As part of that pass, `RELEASE_NOTES.md` was upgraded to introduce `v4.0.0-phase4-demo` as the current release-facing baseline, and `_private/publication_message_v2_20260519.md` was rewritten so the publication narrative now reflects the closed Phase 4 demo state instead of stopping at the older V2C-only candidate framing.
- The release/publication pass was then extended with two ready-to-use packaging artifacts in `_private/`: `github_repo_positioning_v4_20260531.md` (repo description / intro / portfolio blurb / topic guidance) and `release_publication_copy_v4_20260531.md` (release title, release body, and short publication paragraph). This keeps the next publication step lightweight and consistent without reopening technical scope.
- Later on 2026-05-31, Alberto requested a professionalism pass before continuing the review: remove the literal `[DEMO — simulated campaign data]` badge wording from the reports and make the calibration result explicit in the diagnostics executive summary. In response, the Phase 4 report generators were updated to use the more professional label `Simulated campaign baseline`, `NB05` was re-executed so `reports/model_diagnostics_20260519.html` now includes an explicit calibration-decision row (winner, mean calibration gap, ranking delta, and conclusion), and `README.md` was tightened to state the calibration winner and the ranking-first conclusion more concretely.
- Alberto then requested a **full README review** because the calibration improvement still was not visually obvious enough. The README top section was therefore reworked more deeply: the status badge now points to the active `v4.0.0-phase4-demo` baseline, the executive summary now opens with the current project truth (`v1 baseline` + `V2C redesign` + `Phase 4 portfolio/demo baseline`), a new `Project state at a glance` table was added, and the key-metrics table now includes explicit calibration winner / ranking-preserving variant / practical interpretation rows so the main analytical improvement is visible immediately instead of being buried deeper in the document.
- Immediately after that, Alberto approved a second editorial pass. The README was then tightened again for portfolio readability: the `Quick Start` status block was rewritten into a cleaner reader-orientation panel, the operational-completion section now mentions the explicit calibration-decision layer in `NB05`, the roadmap sequencing was updated to reflect that Phase 4 is already closed rather than upcoming, the future-work wording no longer implies that `v4.0.0` is still only hypothetical, and the `Version Note` now names the active baseline directly as `v4.0.0-phase4-demo`.

## Risks / open questions
- The 2026-08-17 P0/staging doubt is now closed at functional/deployment-evidence level, but one narrower operational incident remains unresolved: whether the original lack of redeploy after P0 was caused by `execApprovals` blockage (`approval-timeout (allowlist-miss)`) or whether rebuild/redeploy simply does not belong to the automatic post-fix flow in this environment. Treat that as a separate host-operation/process issue, not as a reopened P0 regression.
- The 2026-08-01 incident is now understood as a resolved cold-path I/O/cache issue in the review-page Python path, not as a remaining Traefik transport defect. If the same symptom ever reappears, re-check the live code path and cache behavior before touching proxy transport again.
- The core v2 risk is now confirmed empirically: even after filtering to repeat customers with 90+ days of tenure, the current adaptive label still produces a very high positive rate and may continue to limit separability.
- The refreshed ROI simulation is still intentionally illustrative and scenario-based; it is stronger for publication/storytelling, but it is not yet a causal or experimentally validated business case.
- The latest run-metadata hardening pass is now synchronized and verified in Drive, but public-release wording should still be reviewed once more before any new GitHub publication step.
- The new n8n support API now runs on port `62881` by user-approved design instead of `62880`, because `62880` is occupied in this runtime by the existing OpenClaw Node server.
- The main Phase 4 measurement risk is no longer confusion about missing production validation; it is the need to keep demo honesty explicit. `retention_events`, holdout lift, and conversion evaluation outputs must be documented as synthetic/simulated until real customer outcome data exists.
- A watchdog script plus persistent OpenClaw cron job now re-check the API every minute so the service comes back automatically after container restarts, provided the OpenClaw gateway itself starts normally.
- There is a methodological risk of over-iterating on too many variants; the benchmark should therefore stay intentionally short (around 3-4 candidate formulations).
- NB06 explainability is still expected to rely on a robust sample rather than full-population SHAP unless runtime permits broader execution.
- n8n remains acceptable for the current orchestration baseline, but a later v3 may replace it with a more hardened automation architecture.
- Phase 2 and the minimum viable Phase 3 layer should preserve explicit comparability against v1 so the portfolio narrative remains traceable.
- The new calibration evidence improves probability-interpretation honesty, but it does not change the core modeling caveat: this remains a ranking-first portfolio baseline unless a future approved line decides to operationalize calibrated probabilities explicitly.


## 2026-08-24
- Current state: Final structural hygiene completed across workspace, Drive Main, local release, and Drive release.
- Last completed step: root cleanup, public-doc relocation, README reconciliation, requirements split, ignore alignment, release curation refresh, and manifest regeneration/verification.
- Next pending step: Project Author final review of clean Drive Main and curated Drive release before local Git integration.
